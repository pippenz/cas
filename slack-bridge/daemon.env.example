# Per-user daemon env — place at ~/.config/cas/env
# Each user gets their own copy with their specific tokens
CAS_SERVE_URL=http://127.0.0.1:18999
CAS_SERVE_TOKEN=your-cas-serve-bearer-token
SLACK_BOT_TOKEN=xoxb-your-bot-token
