# cas-code-review: cas-bae1 (branch visibility)

Report-only review of factory/fast-octopus-98 @ 7958b1c vs epic base b4dbb6e.
FIX ROUND — P1 sync git subprocesses block async daemon loop; P1 ahead/behind parse untested (swap risk); P2s: wrong supervisor-branch fallback on worker panes, duplicated truncation, coverage gaps.

```json
{
  "date": "2026-07-07",
  "task": "cas-bae1",
  "branch": "factory/fast-octopus-98",
  "commit": "7958b1c",
  "base_sha": "b4dbb6e",
  "mode": "report-only",
  "verdict": "FIX ROUND \u2014 P1 sync git subprocesses block async daemon loop; P1 ahead/behind parse untested (swap risk); P2s: wrong supervisor-branch fallback on worker panes, duplicated truncation, coverage gaps.",
  "envelope": {
    "residual": [
      {
        "title": "ahead_behind() git output parsing has zero test coverage",
        "severity": "P1",
        "file": "cas-cli/src/ui/factory/app/branch_visibility.rs",
        "line": 195,
        "why_it_matters": "ahead_behind() parses `git rev-list --left-right --count base...branch` output where the left column is the base-only count (branch's behind count) and the right column is the branch-only count (branch's ahead count). The code assigns the first parsed value to a variable literally named `behind` and the second to `ahead`, then returns `(ahead, behind)` \u2014 a subtle, easy-to-invert mapping. There is no unit test (e.g. against a scratch git repo with a known ahead/behind delta) exercising this function, so a regression that swaps the two fields (displaying a branch as 3 ahead/1 behind when it is actually 1 ahead/3 behind) would pass the full test suite silently and only be caught by a human staring at the radar sidecar.",
        "autofix_class": "manual",
        "owner": "downstream-resolver",
        "confidence": 0.85,
        "evidence": [
          "let mut parts = stdout.split_whitespace(); let behind = parts.next()?.parse::<u32>().ok()?; let ahead = parts.next()?.parse::<u32>().ok()?; Some((ahead, behind))",
          "No test in the diff invokes ahead_behind(), current_branch(), detect_trunk(), or ref_exists() \u2014 only the pure formatting/cache-state helpers (truncate_branch_middle, format_pane_title_with_branch, is_fresh/branch_for_path via manual cache mutators) are tested."
        ],
        "pre_existing": false
      },
      {
        "title": "Synchronous git subprocess spawns block the async factory daemon loop",
        "severity": "P1",
        "file": "cas-cli/src/ui/factory/app/branch_visibility.rs",
        "line": 48,
        "why_it_matters": "BranchVisibilityCache::refresh() shells out via std::process::Command::output() (current_branch for supervisor + every worker, detect_trunk's symbolic-ref/show-ref, and ahead_behind's git rev-list per epic branch) entirely synchronously. It is invoked from FactoryApp::refresh_branch_visibility_cache() (mod.rs:836-859), which runs inside FactoryApp::refresh_data(), which in turn is called directly (no spawn_blocking/tokio::task::spawn_blocking) from the async daemon tick loop at daemon/runtime/lifecycle.rs:333. That same loop body has an explicit comment two lines above (\"Process pending spawns (non-blocking: git ops run on background thread)\") showing the codebase already knows blocking git calls must not run inline on this executor. Every ~10s (the cache TTL), this new code performs N_workers+3+M_epics blocking process spawns on the loop that also drives prompt-queue polling and pending-spawn processing, stalling both for the duration of every git fork/exec in that batch.",
        "autofix_class": "manual",
        "owner": "human",
        "confidence": 0.85,
        "evidence": [
          "cas-cli/src/ui/factory/app/branch_visibility.rs: current_branch/detect_trunk/ahead_behind all use `Command::new(\"git\")...output()` synchronously inside `refresh()`",
          "cas-cli/src/ui/factory/app/mod.rs:836-859 refresh_branch_visibility_cache() calls `self.branch_visibility.refresh(...)` unconditionally whenever refresh_data() doesn't early-return",
          "cas-cli/src/ui/factory/daemon/runtime/lifecycle.rs:318 comment: \"Process pending spawns (non-blocking: git ops run on background thread)\" immediately preceding the synchronous `self.app.refresh_data()` call at line 333"
        ],
        "pre_existing": false
      },
      {
        "title": "Middle-ellipsis truncation logic duplicated verbatim across two new files",
        "severity": "P2",
        "file": "cas-cli/src/ui/factory/app/branch_visibility.rs",
        "line": 108,
        "why_it_matters": "truncate_branch_middle (branch_visibility.rs:108-133) and truncate_middle (status_bar.rs:590-616) implement the identical algorithm (same len/max_chars<=1/max_chars<=4/head-tail-split branches, same variable roles just renamed) introduced in the same change. A future bugfix (e.g. a grapheme-cluster edge case, or an off-by-one in the head/tail split) will predictably be applied to only one copy, leaving the other silently wrong. The repo already has a pattern of one-off truncate helpers per module (mission_workers.rs::truncate, tasks.rs::truncate, reminders.rs::truncate_message, boot/screen.rs::truncate_path, status_bar.rs::truncate_with_ellipsis); this PR adds a second brand-new implementation of the same middle-ellipsis behavior instead of sharing one function between the two call sites it just created.",
        "autofix_class": "gated_auto",
        "owner": "review-fixer",
        "confidence": 0.88,
        "evidence": [
          "branch_visibility.rs:108-133 pub(crate) fn truncate_branch_middle(branch: &str, max_chars: usize) -> String { ... side = (max_chars - 1) / 2 ... }",
          "status_bar.rs:590-616 fn truncate_middle(text: &str, max_chars: usize) -> String { ... head_len = (max_chars - 1) / 2 ... } \u2014 byte-for-byte the same control flow as truncate_branch_middle, just renamed locals (text/head_len/tail_len vs branch/side/tail)"
        ],
        "pre_existing": false
      },
      {
        "title": "Worker pane title falls back to supervisor's branch on cache miss",
        "severity": "P2",
        "file": "cas-cli/src/ui/factory/app/mod.rs",
        "line": 881,
        "why_it_matters": "When a worker has a worktree path but that path isn't yet in the BranchVisibilityCache (e.g. right after a worker is added, before its worktree is created on disk, or after a transient git failure), sync_worker_pane_branch_titles() silently falls back to the *supervisor's* current branch and labels the worker pane with it. This isn't a 'no data' degrade \u2014 it substitutes unrelated, actively wrong information for the exact thing this feature exists to show accurately. The sibling function focused_pane_branch() (same file, lines 822-829) handles the identical cache-miss case correctly by returning None with no misleading substitution, showing this fallback was not the intended pattern.",
        "autofix_class": "gated_auto",
        "owner": "review-fixer",
        "confidence": 0.8,
        "evidence": [
          "mod.rs: `.and_then(|path| { self.branch_visibility.branch_for_path(&path).map(str::to_string) }).or_else(|| { self.branch_visibility.branch_for_path(&self.project_dir).map(str::to_string) });` \u2014 falls back to project_dir's branch whenever the worker-specific lookup misses, not only when worktree_manager is None",
          "worktree/manager/worker_ops.rs:39-41 `pub fn worktree_path_for_worker(&self, worker_name: &str) -> PathBuf { self.worktree_root().join(worker_name) }` \u2014 pure path arithmetic, returns a path regardless of whether the worktree/git repo actually exists yet, so a freshly-added worker's path is guaranteed to miss the cache until the next successful git lookup",
          "epic_workers.rs calls `self.worker_names.push(worker_name.clone()); ... self.sync_worker_pane_branch_titles();` immediately (lines ~414-426 and ~688-697), i.e. right after adding a worker and before any worktree creation/git refresh cycle has populated that worker's cache entry",
          "focused_pane_branch (mod.rs:822-829) computes `path` from worktree_manager only when Some and does not fall back to project_dir in that branch, returning None on cache miss instead of substituting the supervisor's branch \u2014 the correct pattern this function should mirror"
        ],
        "pre_existing": false,
        "requires_verification": true,
        "suggested_fix": "Drop the `.or_else` fallback to project_dir's branch inside the `.and_then` chain when worktree_manager is Some; only use project_dir's branch as the worker's label when there is no worktree_manager at all (single-repo/non-worktree mode), matching focused_pane_branch's logic."
      },
      {
        "title": "sync_worker_pane_branch_titles() fallback-to-project_dir path is untested",
        "severity": "P2",
        "file": "cas-cli/src/ui/factory/app/mod.rs",
        "line": 869,
        "why_it_matters": "This new method builds a per-worker branch map with an `.or_else` fallback to the supervisor's project_dir branch when the worker's own worktree lookup misses, then rewrites every worker pane title via format_pane_title_with_branch. There is no test file covering FactoryApp's app/mod.rs additions (focused_pane_branch, focused_epic_branch_status, refresh_branch_visibility_cache, sync_worker_pane_branch_titles), so a regression that drops the fallback, mismatches worker_names against mux pane ids, or fails to call set_title would not be caught \u2014 worker panes would silently keep stale/default titles and no test would fail.",
        "autofix_class": "manual",
        "owner": "downstream-resolver",
        "confidence": 0.8,
        "evidence": [
          "pub(crate) fn sync_worker_pane_branch_titles(&mut self) { let worker_branches: HashMap<String, Option<String>> = self.worker_names.iter().map(|worker| { ... .or_else(|| self.branch_visibility.branch_for_path(&self.project_dir).map(str::to_string)) }); for pane in self.mux.panes_mut() { if *pane.kind() == PaneKind::Worker { ... pane.set_title(format_pane_title_with_branch(pane.id(), branch)); } } }",
          "No test symbol referencing sync_worker_pane_branch_titles, focused_pane_branch, or focused_epic_branch_status appears anywhere in the diff."
        ],
        "pre_existing": false
      },
      {
        "title": "Epic ahead/behind computed for every historical epic, not just the displayed one",
        "severity": "P2",
        "file": "cas-cli/src/ui/factory/app/mod.rs",
        "line": 844,
        "why_it_matters": "refresh_branch_visibility_cache() builds `epic_branches` from `self.director_data.epic_tasks` before the per-session filter (filter_director_agents_to_current_session, called later at mod.rs:680) runs, and `epic_tasks` is populated in cas-factory's DirectorData::load_with_stores by pushing every task with TaskType::Epic regardless of status (crates/cas-factory/src/director.rs:238-239) \u2014 i.e. every epic ever created in the project's task store, open or closed. Yet only one value is ever read back: FactoryApp::focused_epic_branch_status() looks up a single `current_epic_id`. So on every 10s cache refresh the code forks a `git rev-list --left-right --count` process per historical epic with a recorded branch, and that cost grows unboundedly with total epic count over a project's lifetime even though at most one result is ever displayed.",
        "autofix_class": "gated_auto",
        "owner": "review-fixer",
        "confidence": 0.8,
        "evidence": [
          "cas-cli/src/ui/factory/app/mod.rs:842-851 builds epic_branches from `self.director_data.epic_tasks` with no status/session filter",
          "crates/cas-factory/src/director.rs:237-239: `if task.task_type == TaskType::Epic { epic_tasks.push(to_summary(task)); }` runs for all tasks regardless of TaskStatus",
          "cas-cli/src/ui/factory/app/mod.rs:831-834 focused_epic_branch_status() only ever reads `self.current_epic_id`, discarding all other computed entries",
          "cas-cli/src/ui/factory/app/mod.rs:680 filter_director_agents_to_current_session() (which would retain only the current epic) runs AFTER refresh_branch_visibility_cache() in refresh_data(), so the unfiltered list is what's used"
        ],
        "pre_existing": false
      },
      {
        "title": "BranchVisibilityCache::refresh() itself is never exercised by a test",
        "severity": "P2",
        "file": "cas-cli/src/ui/factory/app/branch_visibility.rs",
        "line": 43,
        "why_it_matters": "The only cache test (cache_ttl_and_miss_degrade) builds state via #[cfg(test)]-only helpers (insert_path_branch, mark_refreshed) that bypass refresh() entirely. refresh()'s own logic \u2014 early-return when is_fresh(now) is true, building next_path_branches from supervisor_path plus every worker_paths entry, computing epic_ahead_behind per epic_branches entry, and swapping in the new maps \u2014 has no direct coverage. A regression such as refresh() failing to early-return under a fresh cache (causing needless git shell-outs every render tick) or forgetting to update refreshed_at would not fail any test.",
        "autofix_class": "manual",
        "owner": "downstream-resolver",
        "confidence": 0.75,
        "evidence": [
          "pub(crate) fn refresh(&mut self, supervisor_path: &Path, worker_paths: &[(String, PathBuf)], epic_branches: &[(String, String)], now: Instant) { if self.is_fresh(now) { return; } ...",
          "#[test] fn cache_ttl_and_miss_degrade() ... cache.insert_path_branch(path.clone(), \"factory/worker\"); cache.mark_refreshed(now); \u2014 never calls refresh()"
        ],
        "pre_existing": false
      },
      {
        "title": "focused_pane_branch() PaneKind branching (Supervisor/Worker/Director/Shell) has no test",
        "severity": "P3",
        "file": "cas-cli/src/ui/factory/app/mod.rs",
        "line": 818,
        "why_it_matters": "focused_pane_branch() has distinct behavior per PaneKind (Supervisor looks up project_dir, Worker looks up the worktree path with a project_dir fallback via unwrap_or, Director/Shell return None). None of these branches is exercised by a test, so e.g. accidentally returning None for PaneKind::Worker or mixing up which path is looked up for Supervisor vs Worker would silently break the status bar's branch label for one pane kind while other coverage stays green.",
        "autofix_class": "manual",
        "owner": "downstream-resolver",
        "confidence": 0.7,
        "evidence": [
          "pub(crate) fn focused_pane_branch(&self) -> Option<&str> { let pane = self.mux.focused()?; match pane.kind() { PaneKind::Supervisor => ..., PaneKind::Worker => { ... }, PaneKind::Director | PaneKind::Shell => None } }"
        ],
        "pre_existing": false
      },
      {
        "title": "format_branch_status narrow-width clamp (branch_budget floor of 4) untested",
        "severity": "P3",
        "file": "cas-cli/src/ui/factory/director/factory_radar.rs",
        "line": 246,
        "why_it_matters": "format_branch_status computes branch_budget as `width - prefix.len() - suffix.len()` clamped to a minimum of 4 via `.max(4)`, which can go negative-then-clamp when width is small (e.g. a narrow radar sidecar). The single new test (factory_radar_renders_epic_branch_ahead_behind) only exercises a wide 110-column terminal; there is no test for a narrow width where the `.max(4)` floor kicks in or where `saturating_sub` would otherwise underflow, so a regression producing a panic-inducing or garbled label on narrow terminals would not be caught.",
        "autofix_class": "manual",
        "owner": "downstream-resolver",
        "confidence": 0.6,
        "evidence": [
          "let branch_budget = (width as usize).saturating_sub(prefix.len() + suffix.chars().count()).max(4);",
          "factory_radar_renders_epic_branch_ahead_behind uses `TestBackend::new(110, 12)` \u2014 the only branch-status rendering test, at a wide width."
        ],
        "pre_existing": false
      }
    ],
    "pre_existing": [],
    "mode": "report-only",
    "intent_summary": "Adds branch-visibility display across three factory TUI surfaces (status bar, worker pane titles, FACTORY radar sidecar) per cas-bae1 (R4-R6). Scope: new BranchVisibilityCache with TTL-based git lookups refreshed off the render path, plus width-aware truncation formatting for narrow terminals. Non-goals: radar layout restructure (blocked sibling cas-2d78) and any coordination/merge logic changes \u2014 this is a read-only display layer over cached data.",
    "activation": {
      "activated": [
        "correctness",
        "testing",
        "maintainability",
        "project-standards",
        "performance",
        "gpt-5.5:independent"
      ],
      "fallow_skipped": true,
      "fallow_skip_reason": "non-JS/TS repo: no package.json and no JS/TS files in diff (all changed files are .rs)",
      "gpt55_independent": true,
      "gpt55_independent_skipped": false,
      "gpt55_independent_skip_reason": null,
      "skipped_personas": [],
      "personas_run": 6
    },
    "stats": {
      "total_findings": 9,
      "p0": 0,
      "p1": 2,
      "p2": 5,
      "p3": 2,
      "personas_run": 6,
      "task_id": "cas-bae1"
    }
  }
}
```
