# cas-code-review: cas-432f (panel scoping + unfocused placeholder)

Report-only review of factory/happy-marten-39 @ 24d9c78 (rebased) vs epic base b5e9fa7.
MERGE WITH FOLLOW-UP — {'P1': 1, 'P2': 5, 'P3': 1} — P1 (over-restrictive session-agent gate blanks focused epic while subtasks unassigned) + P2 test gaps folded into cas-c9f6, which gates the epic->main merge, so main never ships the P1.

```json
{
  "date": "2026-07-06",
  "task": "cas-432f",
  "branch": "factory/happy-marten-39",
  "commit": "24d9c78",
  "base_sha": "b5e9fa7",
  "mode": "report-only",
  "verdict": "MERGE WITH FOLLOW-UP \u2014 {'P1': 1, 'P2': 5, 'P3': 1} \u2014 P1 (over-restrictive session-agent gate blanks focused epic while subtasks unassigned) + P2 test gaps folded into cas-c9f6, which gates the epic->main merge, so main never ships the P1.",
  "envelope": {
    "residual": [
      {
        "title": "Session-agent gate blanks the focused epic before its first subtask is assigned",
        "severity": "P1",
        "file": "cas-cli/src/ui/factory/director/tasks.rs",
        "line": 29,
        "why_it_matters": "This diff introduces a new requirement, layered on top of the existing epic-ID match, that at least one of the focused epic's ready/in-progress subtasks already have an assignee resolvable in agent_id_to_name (task_assigned_to_session_agent / epic_has_session_agent_subtask) before the epic is treated as 'really' focused. But base commit b5e9fa7's own event detector fires EpicStarted (and sets current_epic_id) on an 'Open-with-branch' epic \u2014 i.e. as soon as the epic exists with a branch, well before any subtask is necessarily assigned to a worker \u2014 and ready_tasks itself is defined as TaskStatus::Open|Blocked (director.rs), a state that commonly has assignee=None until a worker claims it or the supervisor explicitly assigns it. The new gate is applied identically in three places wired by this diff: ScopedTaskView's epic-group filter (tasks.rs, hides the whole epic group from the TASKS panel), factory_radar.rs's render_epic_progress (hides the FACTORY radar epic block), and mission_epic.rs's render_status_strip (hides the top status-strip epic info). So during the normal, common window between 'epic focused/branch created' and 'first subtask assigned' \u2014 or any later window where all of an epic's remaining subtasks are momentarily unassigned (e.g. one just closed, next not yet claimed) \u2014 the supervisor's epic identity and progress disappear simultaneously from all three surfaces and fall back to the 'No focused epic' placeholder, even though current_epic_id correctly points at the real, active epic. The PR's own non-goal note ('known-accepted over-broad session-agent gate reconciliation, tracked separately as cas-2819') suggests the authors know the gate has issues, but that note reads as describing an over-permissive gate, not this over-restrictive false-negative; a human should confirm cas-2819 actually covers this exact failure mode before treating it as already tracked.",
        "autofix_class": "manual",
        "owner": "human",
        "confidence": 0.68,
        "evidence": [
          "tasks.rs ScopedTaskView::new: `group.epic.id == epic_id && group.subtasks.iter().any(|task| task_assigned_to_session_agent(task, data))` \u2014 drops the entire epic group unless a subtask is already assigned.",
          "tasks.rs task_assigned_to_session_agent: `task.assignee.as_ref().is_some_and(...)` returns false for assignee: None, which is the normal state of TaskStatus::Open (ready) tasks per crates/cas-factory/src/director.rs's `TaskStatus::Open | TaskStatus::Blocked => ready_tasks.push(...)`.",
          "factory_radar.rs: `if !super::tasks::epic_has_session_agent_subtask(data, focused_epic_id) { render_unfocused_epic_placeholder(...); return; }` applies the same gate to the FACTORY radar epic block.",
          "mission_epic.rs: `let active_epic = focused_epic_id.filter(|epic_id| super::tasks::epic_has_session_agent_subtask(data, epic_id)).and_then(...)` applies the same gate to the top status strip.",
          "events.rs comments confirm EpicStarted fires for an 'Open-with-branch' epic (score by active-subtask counts, pick_best_open_branch_epic), i.e. epic focus is established independent of any subtask having an assignee yet.",
          "The diff's own test `scoped_task_view_rejects_focus_without_session_agent_subtasks` only exercises a foreign epic whose subtask is assigned to 'other-worker' \u2014 it never covers the focused epic's own subtasks all being assignee: None."
        ],
        "pre_existing": false,
        "requires_verification": true,
        "suggested_fix": "Relax the gate so a subtask that simply belongs to the focused epic (task.epic == Some(epic_id)) is sufficient, regardless of assignee; reserve the assignee/session-agent check for disambiguating standalone (epic-less) tasks or as a secondary staleness signal combined with epic provenance, not as the sole criterion for epic visibility."
      },
      {
        "title": "render_status_strip's new session-agent gate on active_epic has zero test coverage",
        "severity": "P2",
        "file": "cas-cli/src/ui/factory/director/mission_epic.rs",
        "line": 35,
        "why_it_matters": "The diff changes `active_epic` selection from a plain `.and_then(find by id)` to `.filter(epic_has_session_agent_subtask).and_then(find by id)`, so the Mission Control status strip now silently hides the epic progress chip whenever the focused epic has no session-agent-assigned subtask. There is no test file for mission_epic.rs at all (confirmed via `grep -n 'mod tests' cas-cli/src/ui/factory/director/mission_epic.rs` returning nothing at base b5e9fa7 and after the diff). If the filter predicate were inverted, dropped, or the epic lookup order swapped, the status strip would either show the wrong epic's progress or hide a legitimate focused epic's progress, and no test would fail.",
        "autofix_class": "manual",
        "owner": "review-fixer",
        "confidence": 0.85,
        "evidence": [
          "mission_epic.rs diff: `let active_epic = focused_epic_id.filter(|epic_id| super::tasks::epic_has_session_agent_subtask(data, epic_id)).and_then(|epic_id| data.epic_tasks.iter().find(|e| e.id == epic_id));`",
          "grep -n 'mod tests|#[test]' cas-cli/src/ui/factory/director/mission_epic.rs returns no matches (verified in worktree checked out at base SHA b5e9fa7)"
        ],
        "pre_existing": false
      },
      {
        "title": "Epic-scoped selection helpers untested for new current_epic_id filtering",
        "severity": "P2",
        "file": "cas-cli/src/ui/factory/app/sidecar_and_selection.rs",
        "line": 546,
        "why_it_matters": "get_selected_task_id/get_selected_task/task_display_item_count were rewritten to build a `ScopedTaskView::new(&self.director_data, self.current_epic_id.as_deref())` instead of the raw `tasks_by_epic()` groups, meaning task-panel row counting, index-to-task-id mapping, and full-task lookup now depend on epic scoping plus the session-agent gate baked into ScopedTaskView. sidecar_and_selection.rs's existing `mod tests` block (lines ~900+) only covers scroll/alt-screen/mission-control-focus behavior; none of its tests construct a FactoryApp with `current_epic_id` set and multiple epics/tasks to verify that `get_selected_task_id`/`get_selected_task` still map the correct display index to the correct task once scoping removes rows, or that `task_display_item_count` matches `tasks::render_with_focus`'s scoped item count. A broken row-index alignment (e.g. only current_epic_id threaded into count but not into id-lookup, or vice versa) would cause the sidecar to open/act on the wrong task and no test would catch it.",
        "autofix_class": "manual",
        "owner": "review-fixer",
        "confidence": 0.85,
        "evidence": [
          "task_display_item_count/get_selected_task_id/get_selected_task all now call `crate::ui::factory::director::tasks::ScopedTaskView::new(&self.director_data, self.current_epic_id.as_deref())`",
          "grep for 'current_epic_id' and these three fn names inside sidecar_and_selection.rs mod tests block returns no hits; the mod tests block (starting at line 900) only contains scroll_* tests"
        ],
        "pre_existing": false
      },
      {
        "title": "get_epic_at_display_index's new epic-scoping via ScopedTaskView has no direct test",
        "severity": "P2",
        "file": "cas-cli/src/ui/factory/app/panels_and_modes.rs",
        "line": 138,
        "why_it_matters": "get_epic_at_display_index switched from iterating raw `epic_groups` to iterating `ScopedTaskView::new(&self.director_data, self.current_epic_id.as_deref()).epic_groups`, so clicking/selecting an epic header row now depends on the focused-epic + session-agent filtering added by this diff. panels_and_modes.rs has no `mod tests` block at all (grep returns nothing), so this index-mapping change \u2014 which determines which epic ID a click on a given display row resolves to \u2014 is entirely unverified. A regression here (e.g. wrong epic group filtered in/out, or index drift vs the scoped subtask count) would make clicking an epic header target the wrong epic silently.",
        "autofix_class": "manual",
        "owner": "review-fixer",
        "confidence": 0.8,
        "evidence": [
          "panels_and_modes.rs: `let scoped = crate::ui::factory::director::tasks::ScopedTaskView::new(&self.director_data, self.current_epic_id.as_deref()); ... for group in &scoped.epic_groups {`",
          "grep -n 'mod tests|#[test]' cas-cli/src/ui/factory/app/panels_and_modes.rs returns no matches"
        ],
        "pre_existing": false
      },
      {
        "title": "New agent-filter helper left private; sidecar_and_selection.rs keeps 4 duplicate inline closures",
        "severity": "P2",
        "file": "cas-cli/src/ui/factory/app/sidecar_and_selection.rs",
        "line": 599,
        "why_it_matters": "The diff extracts `task_matches_agent_filter` in tasks.rs specifically to de-duplicate the `match agent_filter { None => true, Some(f) => t.assignee.as_deref() == Some(f) }` pattern (used in task_display_item_count, which this same diff now routes through ScopedTaskView::visible_row_count), but the helper is left module-private (`fn`, not `pub(crate) fn`) so it can't be reused outside tasks.rs. The identical inline closure remains duplicated 4x in get_selected_task_id and get_selected_task in this very file, which this same diff already touches (to swap in `scoped.epic_groups`/`scoped.standalone`). Any future change to the agent-filter semantics (e.g. case sensitivity, id-vs-name matching) now has to be updated in 5 separate places instead of 1.",
        "autofix_class": "gated_auto",
        "owner": "review-fixer",
        "confidence": 0.72,
        "evidence": [
          "cas-cli/src/ui/factory/director/tasks.rs:92 `fn task_matches_agent_filter(task: &TaskSummary, agent_filter: Option<&str>) -> bool` is module-private",
          "cas-cli/src/ui/factory/app/sidecar_and_selection.rs:571,599,637,672 each still inline `match agent_filter { None => true, Some(filter) => t.assignee.as_deref() == Some(filter) }`",
          "the diff already rewrites the surrounding code in get_selected_task_id/get_selected_task to use `scoped.epic_groups`/`scoped.standalone` but stops short of reusing the new predicate"
        ],
        "pre_existing": false
      },
      {
        "title": "Epic-focus resolution + gate duplicated across factory_radar.rs and mission_epic.rs",
        "severity": "P2",
        "file": "cas-cli/src/ui/factory/director/factory_radar.rs",
        "line": 140,
        "why_it_matters": "Two files independently reimplement the same rule (\"a focused epic is only valid if it exists in epic_tasks AND has a session-agent subtask\") with different control flow: factory_radar.rs uses two chained `let Some(..) else { placeholder; return }` guards plus a separate `if !epic_has_session_agent_subtask` check (3 branches), while mission_epic.rs expresses the same rule as `focused_epic_id.filter(gate).and_then(find)` in one expression. Both call the shared `epic_has_session_agent_subtask` helper but re-derive the rest of the resolution logic locally. The diff's own non-goals note the session-agent gate is 'known-accepted over-broad' and slated for reconciliation (cas-2819) \u2014 with the logic duplicated in two places, that future fix has two call sites to keep in sync instead of one.",
        "autofix_class": "manual",
        "owner": "review-fixer",
        "confidence": 0.6,
        "evidence": [
          "cas-cli/src/ui/factory/director/factory_radar.rs:140-152 three sequential guards (focused_epic_id present, epic found in epic_tasks, epic_has_session_agent_subtask) each calling render_unfocused_epic_placeholder",
          "cas-cli/src/ui/factory/director/mission_epic.rs:35-36 `focused_epic_id.filter(|epic_id| super::tasks::epic_has_session_agent_subtask(data, epic_id)).and_then(|epic_id| data.epic_tasks.iter().find(|e| e.id == epic_id))`",
          "no shared `resolve_focused_epic()`-style helper exists in tasks.rs despite tasks.rs already housing the shared gate predicate"
        ],
        "pre_existing": false
      },
      {
        "title": "New focused_epic_id state-vs-param precedence in render_with_state is untested",
        "severity": "P3",
        "file": "cas-cli/src/ui/factory/director/mod.rs",
        "line": 235,
        "why_it_matters": "render_with_state now computes `let focused_epic_id = state.as_ref().and_then(|s| s.focused_epic_id).or(focused_epic_id);` before passing it down to tasks::render_with_focus, i.e. SidecarState's focused_epic_id takes priority over the function parameter, falling back only when state is None or state.focused_epic_id is None. mod.rs has no test module at all. If this precedence were reversed, or the `.or()` fallback dropped, the sidecar's task panel could silently stop scoping to the session's focused epic (reverting to the pre-fix unrelated-epic leakage this diff is meant to fix) with no test failing.",
        "autofix_class": "manual",
        "owner": "review-fixer",
        "confidence": 0.75,
        "evidence": [
          "mod.rs diff: `let focused_epic_id = state.as_ref().and_then(|s| s.focused_epic_id).or(focused_epic_id);` feeding into `tasks::render_with_focus(..., focused_epic_id, ...)`",
          "grep -n 'mod tests|#[test]' cas-cli/src/ui/factory/director/mod.rs returns no matches"
        ],
        "pre_existing": false
      }
    ],
    "pre_existing": [],
    "mode": "report-only",
    "intent_summary": "Goal: scope the factory TUI's TASKS panel and FACTORY radar epic-progress display to the session's currently focused epic, replacing unrelated-epic leakage with an explicit muted placeholder when no valid focused epic exists. Scope: adds ScopedTaskView (tasks.rs) filtering epic_groups/standalone to the focused epic + session-agent-assigned tasks, threads focused_epic_id through render_with_focus/render_with_state call sites (panels_and_modes.rs, rendering/core.rs, sidecar_and_selection.rs, mod.rs), and adds the unfocused placeholder + session-agent gate to factory_radar.rs/mission_epic.rs. Non-goals: explicit epic pinning (future), and the known-accepted over-broad session-agent gate reconciliation (tracked separately as cas-2819) is not addressed here.",
    "activation": {
      "activated": [
        "correctness",
        "testing",
        "maintainability",
        "project-standards",
        "adversarial",
        "gpt-5.5:independent"
      ],
      "fallow_skipped": true,
      "fallow_skip_reason": "non-JS/TS repo: Rust cargo workspace (cas-cli), no package.json and no JS/TS files in diff",
      "gpt55_independent": true,
      "gpt55_independent_skipped": false,
      "gpt55_independent_skip_reason": null,
      "skipped_personas": [],
      "personas_run": 6
    },
    "stats": {
      "total_findings": 7,
      "p0": 0,
      "p1": 1,
      "p2": 5,
      "p3": 1,
      "personas_run": 6,
      "task_id": "cas-432f"
    }
  }
}
```
