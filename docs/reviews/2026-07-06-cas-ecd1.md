# cas-code-review: cas-ecd1 (focused-epic resolution)

Report-only review of factory/fast-octopus-98 @ b5e9fa7 vs epic base 82213cd.
Verdict: ALLOW (no P0/P1). Follow-up task filed for P2 cluster.

```json
{
  "date": "2026-07-06",
  "task": "cas-ecd1",
  "branch": "factory/fast-octopus-98",
  "commit": "b5e9fa7",
  "base_sha": "82213cd002b4a6ad2da13cdaefd9904a6f8b0c50",
  "mode": "report-only",
  "verdict": "ALLOW \u2014 0 P0, 0 P1; 7 P2 + 1 P3 residual (test coverage + helper-reuse hygiene), 0 pre-existing",
  "envelope": {
    "residual": [
      {
        "title": "handle_epic_events duplicates set_active_epic instead of reusing it; no test locks persistence",
        "severity": "P2",
        "file": "cas-cli/src/ui/factory/app/render_and_ops/epic_workers.rs",
        "line": 137,
        "why_it_matters": "handle_epic_events (the production DirectorEvent::EpicStarted handler invoked from daemon/runtime/lifecycle.rs) now manually replicates set_active_epic's field-setting plus a direct persist_current_epic_id() call rather than calling the new set_active_epic helper introduced in app/mod.rs. No test in the codebase invokes handle_epic_events and asserts current_epic_id (or the persisted session metadata) afterward \u2014 the closest existing test (epic_state_update_before_filter_retains_new_epic_tasks) manually re-implements the epic_state transition inline rather than calling handle_epic_events. If a future edit drops the persist_current_epic_id() call from this duplicated path (while the refresh-loop path in app/mod.rs still calls set_active_epic correctly), no test would catch that the daemon-driven epic start silently stops persisting the session's active epic.",
        "autofix_class": "manual",
        "owner": "review-fixer",
        "confidence": 0.85,
        "evidence": [
          "epic_workers.rs diff: '+ self.current_epic_id = Some(epic_id.clone()); + self.persist_current_epic_id();' added inline instead of calling `self.set_active_epic(epic_id, epic_title)` which was introduced in app/mod.rs for the same event type",
          "grep for `handle_epic_events(` shows it is called from cas-cli/src/ui/factory/daemon/runtime/lifecycle.rs:351 (a real production path), but no test file calls it directly and asserts current_epic_id or persisted metadata"
        ],
        "pre_existing": false
      },
      {
        "title": "preferred_epic_id_from_session_metadata_named read/filter logic has no direct unit test",
        "severity": "P2",
        "file": "cas-cli/src/ui/factory/app/mod.rs",
        "line": 1193,
        "why_it_matters": "Only the write-side helper (persist_session_metadata_epic_id_at) gets a roundtrip test (persist_session_metadata_epic_id_roundtrips_existing_metadata). The read-side function that actually supplies preferred_epic_id to detect_epic_state at every call site (init.rs x2, fork_first.rs) \u2014 including its whitespace/empty-string filtering (`.filter(|id| !id.trim().is_empty())`) and its silent None-fallback on missing file or malformed JSON \u2014 has no test at all. A regression that drops the trim/empty check, or that returns Some(\"\") instead of None, would not surface in CI even though it changes which epic the whole session focuses on.",
        "autofix_class": "manual",
        "owner": "review-fixer",
        "confidence": 0.85,
        "evidence": [
          "mod.rs diff adds `pub(crate) fn preferred_epic_id_from_session_metadata_named(session_name: &str) -> Option<String> { ... metadata.epic_id.filter(|id| !id.trim().is_empty()) }` with no accompanying test",
          "only test added in the same diff hunk is `persist_session_metadata_epic_id_roundtrips_existing_metadata`, which exercises persist_session_metadata_epic_id_at, not preferred_epic_id_from_session_metadata_named"
        ],
        "pre_existing": false
      },
      {
        "title": "Focused-epic gating in render_epic_progress/has_epic changed semantics with zero test coverage",
        "severity": "P2",
        "file": "cas-cli/src/ui/factory/director/factory_radar.rs",
        "line": 79,
        "why_it_matters": "has_epic and render_epic_progress changed from 'show progress if any epic_tasks exist (InProgress-or-first)' to 'show progress only if focused_epic_id matches an entry in data.epic_tasks'. This is the same picker area previously patched for a mid-session epic hijack bug (cas-4181, referenced in the surrounding comments), yet neither factory_radar.rs nor its near-duplicate in mission_epic.rs (render_status_strip, line ~35) has any test module. A wiring bug anywhere upstream (current_epic_id staying None after a valid epic starts, or the id going stale) would make the epic progress bar silently disappear from the radar/status-strip with no failing test to flag it, and the two render sites could silently diverge from each other since they reimplement the same `focused_epic_id.and_then(...)` lookup independently.",
        "autofix_class": "manual",
        "owner": "review-fixer",
        "confidence": 0.75,
        "evidence": [
          "factory_radar.rs diff: `let has_epic = focused_epic_id.is_some_and(|epic_id| data.epic_tasks.iter().any(|epic| epic.id == epic_id));` replacing the old `!data.epic_tasks.is_empty()` check, with no `mod tests` present in the file",
          "mission_epic.rs diff independently reimplements the same `focused_epic_id.and_then(|epic_id| data.epic_tasks.iter().find(|e| e.id == epic_id))` lookup, also with zero test coverage in that file",
          "grep across both files for '#[test]'/'mod tests' returns no matches"
        ],
        "pre_existing": false
      },
      {
        "title": "New set_active_epic helper not reused at the analogous EpicStarted call site",
        "severity": "P2",
        "file": "cas-cli/src/ui/factory/app/render_and_ops/epic_workers.rs",
        "line": 138,
        "why_it_matters": "The diff extracts a `set_active_epic(epic_id, epic_title)` helper in mod.rs that bundles `current_epic_id = Some(...)`, `epic_state = Active {...}`, and `persist_current_epic_id()` specifically to keep those three steps in sync. `handle_epic_events`'s own `EpicStarted` arm \u2014 the other place an epic transitions to Active in this same PR \u2014 was left as a hand-inlined copy of the same three steps (via `mem::replace` + manual `current_epic_id` assignment + `persist_current_epic_id()`) instead of calling the new helper. Any future change to what 'starting an epic' means (e.g. additional bookkeeping, error handling around persist) has two divergent implementations to update, and they can already silently drift.",
        "autofix_class": "manual",
        "owner": "review-fixer",
        "confidence": 0.75,
        "evidence": [
          "mod.rs adds `fn set_active_epic(&mut self, epic_id: &str, epic_title: &str) { self.current_epic_id = ...; self.epic_state = EpicState::Active {...}; self.persist_current_epic_id(); }` and calls it from handle_events's EpicStarted match",
          "epic_workers.rs EpicStarted arm instead does `let previous = std::mem::replace(&mut self.epic_state, EpicState::Active {...}); self.current_epic_id = Some(epic_id.clone()); self.persist_current_epic_id();` \u2014 same three effects, separately written",
          "the only reason it wasn't reused as-is is that this call site also needs `previous` for EpicStateChange::Started, which set_active_epic doesn't return \u2014 an easy fix (have set_active_epic return the replaced EpicState) was not made"
        ],
        "pre_existing": false
      },
      {
        "title": "reset_epic_state() never clears current_epic_id or persisted session-metadata epic id",
        "severity": "P2",
        "file": "cas-cli/src/ui/factory/app/render_and_ops/epic_workers.rs",
        "line": 176,
        "why_it_matters": "This diff promotes current_epic_id from an internal event-detection hint into (a) the sole driver of factory_radar/mission_epic rendering and (b) a value persisted cross-restart via persist_current_epic_id()/session metadata. set_active_epic() keeps epic_state and current_epic_id (+ the on-disk copy) in lockstep when an epic starts, but its sibling reset_epic_state() \u2014 doc-commented 'Reset epic state to idle (after merge completes)' \u2014 only does `self.epic_state = EpicState::Idle`; current_epic_id stays Some(old_epic_id) and the persisted metadata file keeps naming the completed epic. reset_epic_state currently has zero callers repo-wide (verified via grep), so the gap is dormant, but the moment it (or any future 'epic merged, go idle' path) is wired up, the epic panel keeps showing the old epic and a restart re-reads the stale id as preferred_epic_id \u2014 reopening exactly the 'ghost current_epic_id freezes TUI' failure mode this same file's cas-4181 test comments (mod.rs:1821-1825) warn about, now amplified by cross-restart persistence.",
        "autofix_class": "manual",
        "owner": "human",
        "confidence": 0.65,
        "evidence": [
          "epic_workers.rs:176-178 `pub fn reset_epic_state(&mut self) { self.epic_state = EpicState::Idle; }` \u2014 no current_epic_id or persistence touch",
          "mod.rs set_active_epic() sets current_epic_id AND calls self.persist_current_epic_id() together \u2014 the two helpers are asymmetric",
          "`grep -rn \"reset_epic_state\" cas-cli/src/` returns only the definition, zero call sites",
          "mod.rs:1821-1825 test doc comment names the exact incident class ('ghost current_epic_id freezes TUI', cas-4181) this gap would reopen"
        ],
        "pre_existing": false,
        "requires_verification": true
      },
      {
        "title": "Modified test drops coverage of preferred_epic_id vs. subtask-heuristic priority (cas-4181-adjacent)",
        "severity": "P2",
        "file": "cas-cli/src/ui/factory/app/mod.rs",
        "line": 1616,
        "why_it_matters": "The existing regression test `detect_epic_preferred_id_takes_priority_over_heuristic` previously proved preferred_epic_id wins even when a rival epic is selected via the subtask-derived heuristic (pick_best_open_branch_epic scoring on in_progress_tasks/ready_tasks, reachable only for Open epics with a branch). The diff's fixture rewrite deletes that in_progress_tasks/subtask setup and instead gives the rival epic a literal TaskStatus::InProgress, which is resolved by an earlier, unrelated branch of detect_epic_state (the direct 'find an in-progress epic' scan). No test now exercises preferred_epic_id short-circuiting the subtask-heuristic branch specifically \u2014 the branch most closely tied to the named cas-4181 hijack incident \u2014 even though that heuristic code (pick_best_open_branch_epic) is unchanged and still fully reachable in production whenever preferred_epic_id is absent or stale.",
        "autofix_class": "manual",
        "owner": "human",
        "confidence": 0.65,
        "evidence": [
          "diff replaces in_progress_tasks:[TaskSummary{epic: Some(active_id), status: InProgress}] (subtask signal feeding pick_best_open_branch_epic scoring) with in_progress_tasks: Vec::new() and instead sets the rival epic task's own status to InProgress",
          "events.rs:1131-1147 pick_best_open_branch_epic filters epic_tasks by `status == TaskStatus::Open && branch.is_some()` and scores via in_progress_tasks/ready_tasks \u2014 a different code path than mod.rs's direct 'status == InProgress' scan that now wins the new fixture",
          "test's own updated comment changed from 'preferred_epic_id ... over subtask heuristic' to 'preferred_epic_id ... over unrelated InProgress epics', confirming the scenario being exercised changed rather than being additive"
        ],
        "pre_existing": false,
        "requires_verification": true
      },
      {
        "title": "persist_current_epic_id has no counterpart clearing epic_id on completion",
        "severity": "P2",
        "file": "cas-cli/src/ui/factory/app/render_and_ops/epic_workers.rs",
        "line": 146,
        "why_it_matters": "The diff makes session-metadata `epic_id` the new preferred source that `detect_epic_state` consults first (returning Active immediately unless the referenced epic's TaskStatus is already Closed). `set_active_epic`/`persist_current_epic_id` write this field on every `EpicStarted`, but nothing clears it \u2014 `DirectorEvent::EpicCompleted` (epic_workers.rs:146) only flips `self.epic_state` to `Completing` and never calls `persist_current_epic_id()` or clears `current_epic_id`; `reset_epic_state()` (mod.rs, sets `self.epic_state = EpicState::Idle`) likewise never touches `current_epic_id` or the persisted file. Grep confirms `current_epic_id` is set to `None` nowhere in non-test code except one struct literal at init.rs:608. Concretely: epic A completes (subtasks done, `EpicCompleted` fires) but its task row is not yet `TaskStatus::Closed` (closing is typically a distinct supervisor action after merge) \u2014 if the factory session restarts in that window, `preferred_epic_id_from_session_metadata()` returns A, and `detect_epic_state` returns `EpicState::Active` for the already-finished epic A (since `epic.status != Closed`), overriding the InProgress/best-open-branch scan that would otherwise surface the real active epic. This directly undermines the change's stated goal of using `current_epic_id` as the reliable single source of truth.",
        "autofix_class": "manual",
        "owner": "review-fixer",
        "confidence": 0.6,
        "evidence": [
          "cas-cli/src/ui/factory/app/render_and_ops/epic_workers.rs:146-166 \u2014 EpicCompleted arm sets `self.epic_state = EpicState::Completing {...}` and pushes `EpicStateChange::Completed`, but never calls `self.persist_current_epic_id()` or clears `self.current_epic_id`",
          "cas-cli/src/ui/factory/app/render_and_ops/epic_workers.rs:176 \u2014 `reset_epic_state()` only does `self.epic_state = EpicState::Idle;`, no touch of `current_epic_id` or metadata file",
          "grep for 'current_epic_id = None' across cas-cli/src/ui/factory/ (excluding tests) returns nothing",
          "cas-cli/src/ui/factory/app/mod.rs detect_epic_state: `if let Some(epic_id) = preferred_epic_id { if let Some(epic) = data.epic_tasks.iter().find(...) { if epic.status != TaskStatus::Closed { return EpicState::Active {...} } } }` \u2014 an unclosed-but-completed epic in persisted metadata wins over the InProgress scan"
        ],
        "pre_existing": false,
        "requires_verification": true
      },
      {
        "title": "fork_first daemon boot epic-persistence round trip is entirely untested",
        "severity": "P3",
        "file": "cas-cli/src/ui/factory/daemon/fork_first.rs",
        "line": 271,
        "why_it_matters": "This is the stated goal of the change for the fork-first/daemon path: read preferred_epic_id from session metadata at boot (preferred_epic_id_from_session_metadata) and immediately write epic_state.epic_id() back into the freshly created session metadata (create_metadata(..., epic_state.epic_id(), ...)) instead of always passing None. fork_first.rs has no test module at all, so there is no test proving the daemon actually carries the session's previously-persisted epic through a restart, nor one guarding against a regression where CAS_FACTORY_SESSION is unset/misread and the daemon silently reverts to the old None-always behavior.",
        "autofix_class": "manual",
        "owner": "review-fixer",
        "confidence": 0.75,
        "evidence": [
          "fork_first.rs diff: `let preferred_epic_id = preferred_epic_id_from_session_metadata(); let epic_state = detect_epic_state(&director_data, preferred_epic_id.as_deref());` and later `epic_state.epic_id(), // was previously `None, // Epic ID loaded later``",
          "grep for '#[test]'/'mod tests' in fork_first.rs returns no matches"
        ],
        "pre_existing": false
      }
    ],
    "pre_existing": [],
    "mode": "report-only",
    "intent_summary": "Goal: make FactoryApp.current_epic_id the single source of truth for the displayed epic, defaulting to the session's persisted epic and eliminating the first-in-list fallback. Scope: wire preferred_epic_id from SessionMetadata.epic_id at all detect_epic_state call sites (init, fork_first daemon), persist epic starts back to session metadata via set_active_epic/persist_current_epic_id, and thread current_epic_id through to factory_radar/mission_epic rendering instead of independent InProgress-or-first DirectorData scans. Non-goals: no change to worker branch naming or EpicCompleted event semantics themselves (only which epic feeds detect_epic_state).",
    "activation": {
      "activated": [
        "correctness",
        "testing",
        "maintainability",
        "project-standards",
        "adversarial",
        "gpt-5.5:independent"
      ],
      "fallow_skipped": true,
      "fallow_skip_reason": "non-JS/TS repo for this diff: cas-src is a Rust workspace and all 9 changed files are .rs under cas-cli/src/ui/factory; the repo's only package.json (slack-bridge/) is untouched",
      "gpt55_independent": true,
      "gpt55_independent_skipped": true,
      "gpt55_independent_skip_reason": "codex exec timed out after 600s (exit 143) without producing output; unable to complete the independent review",
      "skipped_personas": [
        {
          "reviewer": "gpt-5.5:independent",
          "reason": "codex exec timed out after 600s (exit 143) without producing output; unable to complete the independent review"
        }
      ],
      "personas_run": 5
    },
    "stats": {
      "total_findings": 8,
      "p0": 0,
      "p1": 0,
      "p2": 7,
      "p3": 1,
      "personas_run": 5,
      "task_id": "cas-ecd1"
    }
  }
}
```
