# cas-code-review: cas-305f (focus_epic supervisor pin)

Report-only review of factory/fast-octopus-98 @ 102743e vs epic base b5e9fa7.
Verdict: FIX ROUND REQUIRED before merge (fix task cas-2819).

RESOLUTION (2026-07-06, supervisor-verified at diff level, commit 1ce4f94): P1 race + P2 hijack-reopen + P3 duplication fixed by shared `update_session_metadata_at` (fs2 exclusive lock on sidecar .lock + temp-file/atomic-rename + symlink refusal); concurrency test interleaves epic_id & pinned_epic_id writes. P1 pin-pickup coverage: apply_session_metadata_focus apply + short-circuit tests added. P2 tests (nonexistent id, clear event, coordination routing w/ clear field) added. P3 closed-epic pin now rejected without mutation. Merged to epic branch @ 1ce4f94.

```json
{
  "date": "2026-07-06",
  "task": "cas-305f",
  "branch": "factory/fast-octopus-98",
  "commit": "102743e",
  "base_sha": "b5e9fa7",
  "mode": "report-only",
  "verdict": "FIX ROUND REQUIRED \u2014 2 P1 (session.json cross-process race; pin-pickup zero coverage), 4 P2, 2 P3",
  "envelope": {
    "residual": [
      {
        "title": "apply_session_metadata_focus (TUI pin pickup) has zero invocation coverage",
        "severity": "P1",
        "file": "cas-cli/src/ui/factory/app/mod.rs",
        "line": 734,
        "why_it_matters": "The stated intent's second half \u2014 'TUI re-reads metadata each refresh to pick up the pin' \u2014 is implemented entirely in this new private method, called only from refresh_data() (line ~660), which itself is only ever invoked from daemon runtime code (lifecycle.rs, client_input.rs), never from any test. The only test that exists (preferred_epic_id_from_metadata_uses_pin_before_session_default) covers the pure precedence helper preferred_epic_id_from_metadata, not this method's actual effects: the early-return short-circuit (`if self.current_epic_id.as_deref() == preferred_epic_id.as_deref() return;`), the call into detect_epic_state, or the resulting mutation of self.current_epic_id/self.epic_state. A regression here \u2014 e.g. an inverted comparison that makes the pin never apply, or a forgotten epic_state update leaving current_epic_id and epic_state out of sync \u2014 would pass every existing test while breaking the pin feature for actual TUI users.",
        "autofix_class": "manual",
        "owner": "downstream-resolver",
        "confidence": 0.85,
        "evidence": [
          "fn apply_session_metadata_focus(&mut self) { let preferred_epic_id = preferred_epic_id_from_session_metadata(); if self.current_epic_id.as_deref() == preferred_epic_id.as_deref() { return; } ... }",
          "grep -rn refresh_data cas-cli/src shows call sites only in daemon/runtime/lifecycle.rs, daemon/runtime/client_input.rs, and epic_workers.rs \u2014 no test file calls FactoryApp::refresh_data() or apply_session_metadata_focus() directly"
        ],
        "pre_existing": false
      },
      {
        "title": "Cross-process lost-update race on session.json between daemon and MCP focus_epic",
        "severity": "P1",
        "file": "cas-cli/src/ui/factory/app/mod.rs",
        "line": 1236,
        "why_it_matters": "persist_session_metadata_pinned_epic_id_at (new, written from the MCP tool-server process handling focus_epic) and persist_session_metadata_epic_id_at (pre-existing, written from the forked factory TUI daemon process via set_active_epic on every EpicStarted event) both do an unsynchronized read-full-file -> mutate-one-field -> write-full-file cycle against the exact same ~/.cas/sessions/<session>.json file, with no file lock (fs2-based locking already exists in cas-cli/src/cli/integrate/lock.rs but isn't reused here). Concrete failure: (1) epic B transitions to InProgress; the daemon's set_active_epic()->persist_current_epic_id() reads session.json {epic_id:A, pinned_epic_id:None}. (2) Concurrently the supervisor calls focus_epic id=cas-X in the MCP process, which independently reads the same file {epic_id:A, pinned_epic_id:None} and writes back {epic_id:A, pinned_epic_id:cas-X}. (3) The daemon's in-flight write (holding its earlier stale read) then completes, writing {epic_id:B, pinned_epic_id:None} and silently dropping the supervisor's pin with no error surfaced anywhere. The reverse interleaving instead silently reverts the just-detected epic_id transition back to A. Either way the whole point of cas-305f (a pin that reliably survives until explicitly cleared) is defeated under realistic concurrent usage, and there is no error path that would ever surface this to a user.",
        "autofix_class": "manual",
        "owner": "human",
        "confidence": 0.65,
        "evidence": [
          "fn persist_session_metadata_pinned_epic_id_at(path, pinned_epic_id) { let data = fs::read_to_string(path)?; let mut metadata = serde_json::from_str::<SessionMetadata>(&data)...; metadata.pinned_epic_id = pinned_epic_id.map(str::to_string); ...; fs::write(path, json) }",
          "fn persist_session_metadata_epic_id_at(path, epic_id) { let data = fs::read_to_string(path)?; ...; metadata.epic_id = Some(epic_id.to_string()); ...; fs::write(path, json) } is called from set_active_epic()->persist_current_epic_id() inside FactoryApp::refresh_data, which runs in the forked daemon process (cas-cli/src/ui/factory/daemon/{process.rs,fork_first.rs,runtime/lifecycle.rs} all call std::env::set_var(\"CAS_FACTORY_SESSION\", ...) at daemon startup in that same process)",
          "factory_focus_epic (new in this diff, cas-cli/src/mcp/tools/service/factory_ops.rs) calls persist_session_metadata_pinned_epic_id_at directly from the MCP tool-serving process; grepping factory_ops.rs at base commit b5e9fa7 shows zero prior references to metadata_path or persist_session_metadata_*, confirming this diff is the first cross-process writer of this file",
          "no flock/advisory lock exists anywhere in session.rs or app/mod.rs around metadata_path, despite the fs2 crate already being used for exactly this purpose in cas-cli/src/cli/integrate/lock.rs"
        ],
        "pre_existing": false
      },
      {
        "title": "focus_epic 'task not found' error branch (nonexistent epic id) is untested",
        "severity": "P2",
        "file": "cas-cli/src/mcp/tools/service/factory_ops.rs",
        "line": 1219,
        "why_it_matters": "test_focus_epic_rejects_missing_and_non_epic_without_mutation only exercises id=None (short-circuits before task_store.get) and id=Some(existing-non-epic-task) (get() succeeds, type check fails). Neither case reaches the `task_store.get(epic_id).map_err(...)` branch for an id that does not exist in the store at all. If that error mapping regressed (e.g. swallowed the error and proceeded with a default/None task), no test would catch it, and the no-mutation guarantee for this specific input is unverified.",
        "autofix_class": "manual",
        "owner": "review-fixer",
        "confidence": 0.85,
        "evidence": [
          "let epic = task_store.get(epic_id).map_err(|e| { ... format!(\"Task not found: {epic_id}: {e}\") })?;",
          "test_focus_epic_rejects_missing_and_non_epic_without_mutation only sets `missing.id = None` and `non_epic.id = Some(task_id)` for a task created via store.add \u2014 no test passes a non-existent id"
        ],
        "pre_existing": false
      },
      {
        "title": "Unlocked metadata read-modify-write races TUI, can reopen cas-4181 hijack",
        "severity": "P2",
        "file": "cas-cli/src/ui/factory/app/mod.rs",
        "line": 1236,
        "why_it_matters": "focus_epic (factory_ops.rs) runs in the MCP server process and calls persist_session_metadata_pinned_epic_id_at, which does read-file -> deserialize -> mutate one field -> write-whole-file with no lock, version check, or atomic rename. The FactoryApp TUI process does the identical unguarded read-modify-write on the SAME file via persist_current_epic_id/persist_session_metadata_epic_id_at every time an EpicStarted event fires (set_active_epic). If a supervisor calls focus_epic/clear at the same moment the TUI transitions epics, whichever writer's read snapshot is older wins and silently reverts the other's field. Concretely: TUI advances epic_id A->B and persists it; MCP's focus_epic read happened before that write, so its write reverts epic_id back to A while only updating pinned_epic_id. `apply_session_metadata_focus` (added at this same commit, mod.rs:734) then reads that reverted epic_id on the next refresh, sees current_epic_id (B) != preferred_epic_id (stale A), and calls detect_epic_state directly -- bypassing the strict-improvement/anti-hijack gating that the EpicStarted event path uses -- silently flipping the TUI's tracked epic back to the stale one. This is exactly the symptom class the file's own cas-4181 regression test guards against, reopened via a new unsynchronized writer.",
        "autofix_class": "manual",
        "owner": "human",
        "confidence": 0.78,
        "evidence": [
          "cas-cli/src/ui/factory/app/mod.rs:1236-1247: persist_session_metadata_pinned_epic_id_at does `let data = fs::read_to_string(path)?; ... metadata.pinned_epic_id = ...; fs::write(path, json)` with no locking, run from the MCP server process via factory_focus_epic",
          "cas-cli/src/ui/factory/app/mod.rs (base b5e9fa7, ~706-727): set_active_epic -> persist_current_epic_id performs the same read-modify-write pattern on epic_id from the separate TUI process on every EpicStarted transition",
          "cas-cli/src/ui/factory/app/mod.rs:734-743: new apply_session_metadata_focus calls detect_epic_state directly (bypassing the event_detector's strict-improvement gate referenced by the cas-4181 comment near mod.rs:659-664) whenever current_epic_id differs from metadata-derived preferred_epic_id",
          "the file's own tail comment: '/// Regression test for cas-4181: factory TUI epic hijack.' documents this exact class of bug was previously shipped and fixed"
        ],
        "pre_existing": false
      },
      {
        "title": "focus_epic 'clear' path never asserts the SupervisorInjected event is recorded",
        "severity": "P2",
        "file": "cas-cli/src/mcp/tools/service/factory_ops.rs",
        "line": 1251,
        "why_it_matters": "record_focus_epic_event branches on epic_id.is_some(): pin path uses EventEntityType::Task with entity_id=epic_id, clear path uses EventEntityType::Session with entity_id=factory_session. test_focus_epic_pins_valid_epic_and_records_activity asserts the event for the pin path, but test_focus_epic_clear_removes_pin_and_preserves_session_default only checks the persisted metadata after clearing \u2014 it never inspects the event store. A regression that drops event emission on clear, or mis-sets entity_type/entity_id for the clear branch, would go undetected.",
        "autofix_class": "manual",
        "owner": "review-fixer",
        "confidence": 0.75,
        "evidence": [
          "let entity_type = if epic_id.is_some() { EventEntityType::Task } else { EventEntityType::Session }; let entity_id = epic_id.unwrap_or(factory_session);",
          "test_focus_epic_clear_removes_pin_and_preserves_session_default only asserts on read_session_metadata(...) after the clear call, with no env.event_store() lookup"
        ],
        "pre_existing": false
      },
      {
        "title": "focus_epic routing through the `coordination` MCP tool (and clear-field forwarding) is untested",
        "severity": "P2",
        "file": "cas-cli/src/mcp/tools/service/mod.rs",
        "line": 524,
        "why_it_matters": "focus_epic is dispatched from two entry points: the `factory` tool (tested directly via factory_req()/env.service.factory(...)) and the `coordination` tool, which converts CoordinationRequest to FactoryRequest via to_factory_request() (crates/cas-mcp/src/types/ops_secondary.rs, new `clear: self.clear,` mapping) before calling factory_focus_epic. All three new tests call env.service.factory(...) directly, bypassing to_factory_request() entirely. If the coordination match arm for 'focus_epic' were missing/misspelled, or `clear` were dropped in to_factory_request(), no test would fail \u2014 only an end-to-end `coordination` action=focus_epic,clear=true call would expose it, and that path has no test.",
        "autofix_class": "manual",
        "owner": "review-fixer",
        "confidence": 0.7,
        "evidence": [
          "\"focus_epic\" => this.factory_focus_epic(factory_req).await, // inside the coordination() match, factory_req built via req.to_factory_request()",
          "impl CoordinationRequest::to_factory_request() adds `clear: self.clear,` in crates/cas-mcp/src/types/ops_secondary.rs",
          "grep of factory_mcp_ops_test.rs shows all three new focus_epic tests call `env.service.factory(Parameters(req))`, none call `env.service.coordination(...)` for action=focus_epic"
        ],
        "pre_existing": false
      },
      {
        "title": "New pinned_epic_id persist function duplicates existing epic_id persist function",
        "severity": "P3",
        "file": "cas-cli/src/ui/factory/app/mod.rs",
        "line": 1236,
        "why_it_matters": "persist_session_metadata_pinned_epic_id_at is a near-verbatim copy of the pre-existing persist_session_metadata_epic_id_at (both: read file -> parse SessionMetadata -> mutate exactly one field -> re-serialize -> write), differing only in which field is set and whether the value is Option<&str> or &str. This is the second such field-specific read-modify-write helper in the same module; a shared update_session_metadata_at(path, |m| ...) mutator would collapse both into one implementation and prevent a third copy being added for the next metadata field.",
        "autofix_class": "advisory",
        "owner": "human",
        "confidence": 0.82,
        "evidence": [
          "pub(crate) fn persist_session_metadata_epic_id_at(path: &std::path::Path, epic_id: &str) -> std::io::Result<()> { let data = fs::read_to_string(path)?; let mut metadata = serde_json::from_str::<SessionMetadata>(&data)...; metadata.epic_id = Some(epic_id.to_string()); ... fs::write(path, json) }",
          "pub(crate) fn persist_session_metadata_pinned_epic_id_at(path: &std::path::Path, pinned_epic_id: Option<&str>) -> std::io::Result<()> { let data = fs::read_to_string(path)?; let mut metadata = serde_json::from_str::<SessionMetadata>(&data)...; metadata.pinned_epic_id = pinned_epic_id.map(str::to_string); ... fs::write(path, json) }"
        ],
        "pre_existing": false
      },
      {
        "title": "focus_epic reports success pinning a Closed epic but the pin is a silent no-op",
        "severity": "P3",
        "file": "cas-cli/src/mcp/tools/service/factory_ops.rs",
        "line": 1227,
        "why_it_matters": "factory_focus_epic only validates task_type == Epic before persisting the pin; it never checks epic.status. detect_epic_state's direct-match branch explicitly excludes Closed epics (`if epic.status != TaskStatus::Closed`), so pinning a closed epic writes pinned_epic_id successfully and returns 'Pinned epic focus to {id}...' to the supervisor, yet the TUI silently falls through to the normal inference cascade and displays a different epic. The supervisor is told the pin succeeded with no signal that display focus did not actually change, which can cause them to act on the wrong epic/worker set in a busy multi-epic session.",
        "autofix_class": "advisory",
        "owner": "human",
        "confidence": 0.65,
        "evidence": [
          "cas-cli/src/mcp/tools/service/factory_ops.rs:1227-1234: only `if epic.task_type != TaskType::Epic { return Err(...) }` is checked, no status/Closed check before persisting",
          "detect_epic_state's direct-match arm silently skips Closed epics and falls through to inference (`if epic.status != TaskStatus::Closed { return EpicState::Active ... }`)",
          "factory_focus_epic returns `Ok(Self::success(format!(\"Pinned epic focus to {epic_id} for factory session {factory_session}\")))` unconditionally after a successful persist, regardless of whether the pin will have any visible effect"
        ],
        "pre_existing": false
      }
    ],
    "pre_existing": [],
    "mode": "report-only",
    "intent_summary": "Goal: add supervisor epic-focus pinning (cas-305f) \u2014 new coordination action `focus_epic` (validates task exists + is TaskType::Epic, persists `pinned_epic_id` to factory session metadata, emits an event) with resolution precedence pin > session-default epic_id > inference, and TUI re-reads metadata each refresh to pick up the pin. Scope: MCP dispatch (factory_ops.rs, service/mod.rs, ops_secondary.rs schema), session metadata model/persistence (protocol.rs, session.rs, app/mod.rs), plus new unit/integration tests. Non-goals: no changes to worker spawning, epic close/verify gating, or SQLite store schema.",
    "activation": {
      "activated": [
        "correctness",
        "testing",
        "maintainability",
        "project-standards",
        "security",
        "adversarial",
        "gpt-5.5:independent"
      ],
      "fallow_skipped": true,
      "fallow_skip_reason": "non-JS/TS repo: Rust crate (cas-cli/crates/cas-mcp), no JS/TS files in diff",
      "gpt55_independent": true,
      "gpt55_independent_skipped": false,
      "gpt55_independent_skip_reason": null,
      "skipped_personas": [],
      "personas_run": 7
    },
    "stats": {
      "total_findings": 8,
      "p0": 0,
      "p1": 2,
      "p2": 4,
      "p3": 2,
      "personas_run": 7,
      "task_id": "cas-305f"
    }
  }
}
```
