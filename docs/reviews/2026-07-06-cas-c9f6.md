# cas-code-review: cas-c9f6 (focus-source reconciliation)

Report-only review of factory/fast-octopus-98 @ f6647f4 vs epic base e282b5b.
HARD BLOCK — 2 P0 (foreignness gate gutted: session-agent clause dropped AND regression test assertions flipped to match; daemon-path handle_epic_events bypasses the gate entirely), 2 P1, 4 P2, 5 P3.

```json
{
  "date": "2026-07-06",
  "task": "cas-c9f6",
  "branch": "factory/fast-octopus-98",
  "commit": "f6647f4",
  "base_sha": "e282b5b",
  "mode": "report-only",
  "verdict": "HARD BLOCK \u2014 2 P0 (foreignness gate gutted: session-agent clause dropped AND regression test assertions flipped to match; daemon-path handle_epic_events bypasses the gate entirely), 2 P1, 4 P2, 5 P3.",
  "envelope": {
    "residual": [
      {
        "title": "inferred_epic_is_displayable drops session-agent check, admits foreign epics",
        "severity": "P0",
        "file": "cas-cli/src/ui/factory/app/mod.rs",
        "line": 1254,
        "why_it_matters": "The new 'foreignness gate' that replaces the deleted epic_has_session_agent_subtask only checks whether an epic has ANY ready/in-progress subtask referencing it \u2014 it drops the task_assigned_to_session_agent(task, data) clause entirely. So an epic actively owned by a completely different concurrent factory session (subtasks assigned to agents not in this session's agent_id_to_name) still passes the 'displayability' check and gets adopted as this session's focused epic, exactly the cross-session hijack the old gate existed to prevent. This is proven by the diff itself: factory_radar.rs's test fixture data_with_unrelated_epic() builds epic 'cas-foreign' whose only subtask is assigned to 'other-agent' (not present in agent_id_to_name, i.e. genuinely foreign), and the diff flips the test assertion from `text.contains(\"No focused epic\")` / `!text.contains(\"EPIC: cas-foreign\")` to the opposite \u2014 using the identical foreign fixture.",
        "autofix_class": "manual",
        "owner": "review-fixer",
        "confidence": 0.88,
        "evidence": [
          "cas-cli/src/ui/factory/app/mod.rs:1254-1259: `pub(crate) fn inferred_epic_is_displayable(data: &DirectorData, epic_id: &str) -> bool { data.in_progress_tasks.iter().chain(data.ready_tasks.iter()).any(|task| task.epic.as_deref() == Some(epic_id)) }` \u2014 no assignee/session check, unlike the removed `epic_has_session_agent_subtask` which required `task_assigned_to_session_agent(task, data)`.",
          "cas-cli/src/ui/factory/director/factory_radar.rs test `data_with_unrelated_epic()`: ready_tasks[0].assignee = Some(\"other-agent\"), agent_id_to_name = {\"session-agent\": \"worker-one\"} \u2014 the fixture is deliberately foreign (assignee not a session agent).",
          "cas-cli/src/ui/factory/director/factory_radar.rs diff renames `factory_radar_renders_unfocused_placeholder_instead_of_foreign_epic` (asserted `No focused epic`, `!contains(\"EPIC: cas-foreign\")`) to `factory_radar_renders_resolved_focused_epic_source_blind` (asserts `contains(\"EPIC: cas-foreign\")`, `!contains(\"No focused epic\")`) using the same foreign fixture.",
          "cas-cli/src/ui/factory/director/tasks.rs: ScopedTaskView::new's epic_groups filter changed from `group.epic.id == epic_id && group.subtasks.iter().any(|task| task_assigned_to_session_agent(task, data))` to bare `group.epic.id == epic_id`."
        ],
        "pre_existing": false,
        "requires_verification": true,
        "suggested_fix": "Restore the session-agent requirement inside inferred_epic_is_displayable (or a shared helper) so Inference-sourced focus still requires at least one subtask assigned to a current-session agent, not merely 'epic has any subtask'."
      },
      {
        "title": "Daemon loop's handle_epic_events bypasses the new displayability gate entirely",
        "severity": "P0",
        "file": "cas-cli/src/ui/factory/app/render_and_ops/epic_workers.rs",
        "line": 121,
        "why_it_matters": "refresh_data() (mod.rs) only calls set_active_epic for an EpicStarted event when inferred_epic_is_displayable(...) returns true, but it still returns the FULL unfiltered events Vec from self.event_detector.detect_changes(...). The daemon runtime loop (lifecycle.rs) immediately re-passes that same events Vec into handle_epic_events, whose EpicStarted arm calls self.set_active_epic(epic_id, epic_title) unconditionally with no displayability check at all. So for the one production code path that actually drives the TUI (the daemon tick), any EpicStarted event \u2014 including one refresh_data() correctly declined to adopt \u2014 still ends up setting current_epic_id/epic_state via the second, ungated call. The entire 'move the gate into resolve_epic_state_for_focus' fix is a no-op on the real runtime path.",
        "autofix_class": "manual",
        "owner": "review-fixer",
        "confidence": 0.87,
        "evidence": [
          "cas-cli/src/ui/factory/app/mod.rs refresh(): `if let DirectorEvent::EpicStarted { epic_id, epic_title } = event && inferred_epic_is_displayable(&self.director_data, epic_id) { self.set_active_epic(epic_id, epic_title); }` ... `Ok((prompts, events))` \u2014 events returned is the raw, unfiltered detect_changes() output, not filtered by the gate.",
          "cas-cli/src/ui/factory/app/render_and_ops/epic_workers.rs:117-128: `DirectorEvent::EpicStarted { epic_id, epic_title } => { let previous = self.set_active_epic(epic_id, epic_title); changes.push(EpicStateChange::Started { ... }); }` \u2014 unconditional, no inferred_epic_is_displayable check.",
          "cas-cli/src/ui/factory/daemon/runtime/lifecycle.rs:330-351: `if let Ok((prompts, events)) = self.app.refresh_data() { ... let changes = self.app.handle_epic_events(&events); ... }` \u2014 the identical events list from refresh_data() is fed straight into handle_epic_events in the same tick."
        ],
        "pre_existing": false,
        "requires_verification": true,
        "suggested_fix": "Either filter EpicStarted events out of the Vec returned by refresh_data() when inferred_epic_is_displayable() rejects them, or move the displayability check into handle_epic_events's EpicStarted arm (the single place set_active_epic is actually invoked) so there is exactly one gated call site instead of two that can disagree."
      },
      {
        "title": "handle_epic_events EpicStarted arm untested; bypasses refresh_data's new displayability gate",
        "severity": "P1",
        "file": "cas-cli/src/ui/factory/app/render_and_ops/epic_workers.rs",
        "line": 121,
        "why_it_matters": "lifecycle.rs calls `self.app.refresh_data()` then immediately calls `self.app.handle_epic_events(&events)` with the SAME event list (daemon/runtime/lifecycle.rs:351). refresh_data's inline loop (app/mod.rs) now gates `set_active_epic` behind `inferred_epic_is_displayable(...)` \u2014 the whole point of this diff. But `handle_epic_events`'s `DirectorEvent::EpicStarted` arm calls `self.set_active_epic(epic_id, epic_title)` unconditionally, with no displayability check. For a non-displayable (foreign/zero-subtask) epic: refresh_data's loop skips set_active_epic (gate fails), but handle_epic_events then runs on the identical event and sets `current_epic_id`/`epic_state` to that same non-displayable epic anyway \u2014 silently re-admitting the exact epic the new gate was built to exclude, and doing so for the real focus state (not just rendering). No test exercises this two-call production sequence: handle_epic_events is unit-tested only for EpicCompleted (test `epic_completed_clears_current_and_session_default_without_clearing_pin`), never for EpicStarted, and refresh_data's gated loop is never combined with a subsequent handle_epic_events call in any test. A regression (or this exact behavior) goes completely undetected.",
        "autofix_class": "manual",
        "owner": "review-fixer",
        "confidence": 0.85,
        "evidence": [
          "cas-cli/src/ui/factory/daemon/runtime/lifecycle.rs:329-351 calls `self.app.refresh_data()` and then `self.app.handle_epic_events(&events)` with the same `events` binding",
          "app/mod.rs refresh loop: `if let DirectorEvent::EpicStarted { epic_id, epic_title } = event && inferred_epic_is_displayable(&self.director_data, epic_id) { self.set_active_epic(epic_id, epic_title); }` \u2014 gated",
          "epic_workers.rs handle_epic_events: `DirectorEvent::EpicStarted { epic_id, epic_title } => { let previous = self.set_active_epic(epic_id, epic_title); ... }` \u2014 no gate at all",
          "grep confirms handle_epic_events has exactly one production caller (lifecycle.rs:351) and its EpicStarted arm is never invoked by any test in the diff or pre-existing suite"
        ],
        "pre_existing": false
      },
      {
        "title": "Live EpicStarted transitions bypass the foreignness gate via hardcoded SessionDefault source",
        "severity": "P1",
        "file": "cas-cli/src/ui/factory/app/render_and_ops/epic_workers.rs",
        "line": 121,
        "why_it_matters": "handle_epic_events' EpicStarted arm calls `self.set_active_epic(epic_id, epic_title)` unconditionally (no inferred_epic_is_displayable check), and set_active_epic (mod.rs) hardcodes `current_epic_source = Some(EpicFocusSource::SessionDefault)` regardless of how the epic was actually discovered. Since resolve_epic_state_for_focus only gates focus.source == Inference (Pinned/SessionDefault go straight through detect_epic_state with no displayability check), any epic that legitimately fires EpicStarted \u2014 including one belonging to a different concurrent factory session sharing the same CAS store, whose subtasks are not assigned to this session's agents \u2014 gets permanently promoted to 'trusted' SessionDefault status and never re-checked. Because current_epic_id also drives filter_director_agents_to_current_session (which filters ready/in-progress/epic tasks down to the current epic id), this doesn't just mis-render an epic header \u2014 it can hide the current session's own real tasks/agents while showing a foreign epic's data, reproducing the class of cross-session epic-hijacking bug this codebase already guards against elsewhere (cas-4181 'hijacker' tests in event_detector). The refresh_data() inline handler in app/mod.rs does get the new `inferred_epic_is_displayable` guard, but handle_epic_events \u2014 invoked on the same events list for EpicStateChange side effects \u2014 does not, so the gate this diff is designed to add is bypassed on the primary live-update path.",
        "autofix_class": "manual",
        "owner": "downstream-resolver",
        "confidence": 0.85,
        "evidence": [
          "epic_workers.rs: `let previous = self.set_active_epic(epic_id, epic_title);` in the EpicStarted match arm, with no displayability/foreignness check before the call",
          "mod.rs set_active_epic: `self.current_epic_source = Some(EpicFocusSource::SessionDefault);` is unconditional, not parameterized by how the transition originated",
          "resolve_epic_state_for_focus: `Some(EpicFocusSource::Pinned | EpicFocusSource::SessionDefault) => detect_epic_state(data, focus.epic_id.as_deref())` \u2014 no displayability check for these two sources, only Inference is gated",
          "mod.rs refresh_data's separate inline EpicStarted handler is the only call site wrapped with `&& inferred_epic_is_displayable(&self.director_data, epic_id)`, per the diff hunk touching that block \u2014 handle_epic_events has no equivalent guard"
        ],
        "pre_existing": false,
        "requires_verification": true
      },
      {
        "title": "preferred_epic_focus_from_metadata's EpicFocusSource tagging has zero direct test coverage",
        "severity": "P2",
        "file": "cas-cli/src/ui/factory/app/mod.rs",
        "line": 1291,
        "why_it_matters": "This function decides whether a session's stored focus is Pinned, SessionDefault, or absent \u2014 that classification is what `resolve_epic_state_for_focus` uses to decide whether the foreignness gate applies at all. The only test on this parsing path is `preferred_epic_id_from_metadata_uses_pin_before_session_default`, which calls the cfg(test)-only `preferred_epic_id_from_metadata` wrapper that discards `.source` and checks only `.epic_id`. The other test that exercises `EpicFocusSource` (`explicit_focus_sources_render_unassigned_window_but_inference_zero_subtask_is_gated`) constructs `SessionEpicFocus` literals by hand, never calling `preferred_epic_focus_from_metadata`. So a regression that mislabels source \u2014 e.g. always returning `EpicFocusSource::Inference` even when `pinned_epic_id` is set, while `epic_id` stays correct \u2014 would pass every existing test, yet would silently re-impose the foreignness/displayability gate on a pinned epic in production.",
        "autofix_class": "manual",
        "owner": "review-fixer",
        "confidence": 0.85,
        "evidence": [
          "preferred_epic_focus_from_metadata(metadata) -> SessionEpicFocus { ... source: Some(EpicFocusSource::Pinned) ... source: Some(EpicFocusSource::SessionDefault) ... }",
          "preferred_epic_id_from_metadata(metadata) is #[cfg(test)]-only and returns `preferred_epic_focus_from_metadata(metadata).epic_id`, dropping .source before any assertion runs",
          "grep for `preferred_epic_focus_from_metadata(` shows only two call sites (the cfg(test) wrapper and preferred_epic_focus_from_session_metadata_named) \u2014 no test calls it directly and inspects the returned .source"
        ],
        "pre_existing": false
      },
      {
        "title": "current_epic_source is write-only state; never consulted anywhere",
        "severity": "P2",
        "file": "cas-cli/src/ui/factory/app/mod.rs",
        "line": 386,
        "why_it_matters": "The diff adds a new FactoryApp field `current_epic_source: Option<EpicFocusSource>` with a doc comment claiming it 'is used while resolving focus so only inference-derived epics are gated.' In fact the gating decision is made entirely inside `resolve_epic_state_for_focus`/`inferred_epic_is_displayable` using a transient `SessionEpicFocus` computed at call time \u2014 `current_epic_source` is assigned in five places (init.rs x3, set_active_epic, apply_session_metadata_focus, EpicCompleted handling, reset_epic_state) but never read/matched/branched-on anywhere in the diff. This is dead state that contradicts its own doc comment and the stated PR goal that 'renderers become source-blind' (if renderers are source-blind, FactoryApp shouldn't need to persist source either). A future maintainer will reasonably assume this field drives behavior and waste time tracing it, or worse, build new logic on top of a field that was never wired up.",
        "autofix_class": "manual",
        "owner": "human",
        "confidence": 0.85,
        "evidence": [
          "cas-cli/src/ui/factory/app/mod.rs: `current_epic_source: Option<EpicFocusSource>,` with comment '... this is used while resolving focus so only inference-derived epics are gated.'",
          "Every occurrence in the diff is an assignment: init.rs `current_epic_source: epic_state.epic_id().map(|_| preferred_epic_focus.source.unwrap_or(EpicFocusSource::Inference))` (x2), `set_active_epic` sets `Some(EpicFocusSource::SessionDefault)`, `apply_session_metadata_focus` sets it from `focus.source`, EpicCompleted handling and `reset_epic_state` set it to `None`",
          "No `if self.current_epic_source`, `match self.current_epic_source`, or getter/accessor appears anywhere in the changed files; the actual gating logic lives in `resolve_epic_state_for_focus` and `inferred_epic_is_displayable`, which take `SessionEpicFocus`/`DirectorData` directly, not `self.current_epic_source`"
        ],
        "pre_existing": false
      },
      {
        "title": "reset_epic_state's new clearing logic has no test and no caller anywhere",
        "severity": "P2",
        "file": "cas-cli/src/ui/factory/app/render_and_ops/epic_workers.rs",
        "line": 163,
        "why_it_matters": "This diff adds `self.current_epic_id = None; self.current_epic_source = None; self.clear_persisted_current_epic_id();` to `reset_epic_state`, but a workspace-wide grep finds zero callers of `reset_epic_state` anywhere (including daemon/runtime) and zero tests referencing it. The doc comment claims it runs 'after merge completes', implying a call site should exist; either the call site was dropped elsewhere (functional gap) or the function is dead code \u2014 either way, the new clearing logic added here is completely unverified and any breakage (e.g. forgetting to clear current_epic_source, or clearing the pin metadata by mistake in a future edit) would be invisible to CI.",
        "autofix_class": "manual",
        "owner": "review-fixer",
        "confidence": 0.8,
        "evidence": [
          "pub fn reset_epic_state(&mut self) { self.epic_state = EpicState::Idle; self.current_epic_id = None; self.current_epic_source = None; self.clear_persisted_current_epic_id(); }",
          "`grep -rn \"reset_epic_state\" --include=*.rs .` across the whole workspace (all Cargo members) returns only the definition itself \u2014 no caller, no test"
        ],
        "pre_existing": false
      },
      {
        "title": "inferred_epic_is_displayable drops the session-agent-assignment check it replaces",
        "severity": "P2",
        "file": "cas-cli/src/ui/factory/app/mod.rs",
        "line": 1249,
        "why_it_matters": "The deleted `epic_has_session_agent_subtask` required a subtask be `task_assigned_to_session_agent(task, data)` (i.e. assigned to one of the current session's own agents) before an inferred epic could display. The new `inferred_epic_is_displayable` only checks `task.epic.as_deref() == Some(epic_id)` for any ready/in-progress task \u2014 no ownership check at all. So even where the (correct) Inference-only gate in resolve_epic_state_for_focus does run, an epic belonging to a completely different concurrent factory session, with subtasks unassigned or assigned to a different session's agents, now satisfies the gate. Combined with finding #1 this leaves effectively no working cross-session isolation check anywhere in the diff.",
        "autofix_class": "manual",
        "owner": "downstream-resolver",
        "confidence": 0.78,
        "evidence": [
          "New: `pub(crate) fn inferred_epic_is_displayable(data: &DirectorData, epic_id: &str) -> bool { data.in_progress_tasks.iter().chain(data.ready_tasks.iter()).any(|task| task.epic.as_deref() == Some(epic_id)) }`",
          "Deleted: `pub(crate) fn epic_has_session_agent_subtask(data: &DirectorData, epic_id: &str) -> bool { ... task.epic.as_deref() == Some(epic_id) && task_assigned_to_session_agent(task, data) }`",
          "New test `explicit_focus_sources_render_unassigned_window_but_inference_zero_subtask_is_gated` only exercises 'zero subtasks' vs 'has an unassigned subtask' for Pinned/SessionDefault sources \u2014 it never exercises the Inference branch against a foreign-session-owned (but non-empty) epic, so the ownership regression is untested"
        ],
        "pre_existing": false,
        "requires_verification": true
      },
      {
        "title": "Byte-identical `task()` test builder duplicated across two new test modules",
        "severity": "P3",
        "file": "cas-cli/src/ui/factory/app/panels_and_modes.rs",
        "line": 1043,
        "why_it_matters": "The 4-argument `fn task(id, task_type, epic, assignee) -> TaskSummary` helper added to `panels_and_modes.rs`'s new test module is byte-for-byte identical to the one added to `sidecar_and_selection.rs`'s test module in the same diff. Both construct the same `TaskSummary` literal with the same `branch: Some(...).filter(...)` epic-branch trick. Since both live under `cas-cli/src/ui/factory/app/`, this is exactly the kind of same-crate duplication that should be a single shared test helper (e.g. in a `#[cfg(test)]` support module) rather than copy-pasted twice in one PR \u2014 any future field added to `TaskSummary` now needs updating in both places.",
        "autofix_class": "advisory",
        "owner": "human",
        "confidence": 0.9,
        "evidence": [
          "cas-cli/src/ui/factory/app/panels_and_modes.rs new test mod: `fn task(id: &str, task_type: TaskType, epic: Option<&str>, assignee: Option<&str>) -> TaskSummary { TaskSummary { id: id.to_string(), title: id.to_string(), status: TaskStatus::Open, priority: Priority::MEDIUM, assignee: assignee.map(str::to_string), task_type, epic: epic.map(str::to_string), branch: Some(format!(\"epic/{id}\")).filter(|_| task_type == TaskType::Epic), updated_at: None } }`",
          "cas-cli/src/ui/factory/app/sidecar_and_selection.rs new test mod adds the identical function body under the same name `task(...)` (same 4 args, same field-for-field construction)"
        ],
        "pre_existing": false
      },
      {
        "title": "current_epic_source field is written in 6+ places but never read anywhere",
        "severity": "P3",
        "file": "cas-cli/src/ui/factory/app/mod.rs",
        "line": 387,
        "why_it_matters": "The new EpicFocusSource tracking (current_epic_source) is meant to record whether the active focus came from a pin, session default, or inference, presumably so later logic can treat sources differently. It is assigned in init.rs (x2), mod.rs set_active_epic/apply_session_metadata_focus, and epic_workers.rs (EpicCompleted/reset), but grep across the whole crate shows zero read sites (no comparison, no match, no Debug/Clone derive on FactoryApp that would touch it). rustc's dead_code lint does not catch fields that are only ever written (a known lint blind spot, confirmed by a minimal repro), so this ships silently. It signals the source-aware design described in the PR intent was never actually wired into any decision point \u2014 the whole distinction is inert at runtime.",
        "autofix_class": "advisory",
        "owner": "human",
        "confidence": 0.85,
        "evidence": [
          "grep -rn \"current_epic_source\" cas-cli/ returns only the field declaration and plain assignment (`= None` / `= Some(...)` / `= epic_state...`) sites \u2014 no `if`, `match`, or read expression anywhere.",
          "FactoryApp does not derive Debug/Clone (checked struct definition at mod.rs:277), so no implicit read via derive macros exists either.",
          "Minimal repro (`cargo build` on a struct field only ever assigned, never read) confirms rustc's dead_code lint does not fire for write-only fields, explaining why this ships without a compiler warning."
        ],
        "pre_existing": false,
        "requires_verification": false,
        "suggested_fix": "Either consult current_epic_source somewhere (e.g. to decide whether apply_session_metadata_focus should re-run the displayability check on an already-adopted SessionDefault focus) or remove the field/tracking if it's genuinely unneeded."
      },
      {
        "title": "New director/ test helpers duplicate an existing factory_radar.rs pattern twice",
        "severity": "P3",
        "file": "cas-cli/src/ui/factory/director/mission_epic.rs",
        "line": 208,
        "why_it_matters": "`factory_radar.rs` already defines a `fn buffer_text(terminal: &Terminal<TestBackend>) -> String` test helper (pre-existing, unchanged by this diff) that flattens the ratatui test backend buffer into a string. This diff adds byte-identical copies of that same helper independently in both `mission_epic.rs` and `director/mod.rs`'s new test modules, plus a matching 3-arg `fn task(id, task_type, epic) -> TaskSummary` builder duplicated in both of those same two files. Three copies of the same rendering-assertion helper (and two copies of the same task builder) scattered across the `director/` module make it easy for the pattern to drift (e.g. one copy gets a bugfix or an extra normalization step and the others don't) and is a clear candidate for a shared `#[cfg(test)]` support module under `director/`.",
        "autofix_class": "advisory",
        "owner": "human",
        "confidence": 0.85,
        "evidence": [
          "cas-cli/src/ui/factory/director/factory_radar.rs (pre-existing, base SHA e282b5b, line 494): `fn buffer_text(terminal: &Terminal<TestBackend>) -> String { terminal.backend().buffer().content().iter().map(|cell| cell.symbol()).collect::<Vec<_>>().join(\"\") }`",
          "cas-cli/src/ui/factory/director/mission_epic.rs new test mod adds the identical `fn buffer_text` body plus a `fn task(id: &str, task_type: TaskType, epic: Option<&str>) -> TaskSummary` builder",
          "cas-cli/src/ui/factory/director/mod.rs new test mod adds the same `fn buffer_text` body plus the same 3-arg `fn task(...)` builder, byte-identical to the one in mission_epic.rs"
        ],
        "pre_existing": false
      },
      {
        "title": "EpicCompleted test sets current_epic_source but never asserts it is cleared",
        "severity": "P3",
        "file": "cas-cli/src/ui/factory/app/mod.rs",
        "line": 2261,
        "why_it_matters": "`epic_completed_clears_current_and_session_default_without_clearing_pin` deliberately seeds `app.current_epic_source = Some(EpicFocusSource::SessionDefault)` before firing `EpicCompleted`, suggesting an intent to verify the field is reset by the handler (epic_workers.rs sets `self.current_epic_source = None;` in that branch). The test's assertions only check `app.current_epic_id` and the persisted session metadata \u2014 `app.current_epic_source` is never read back. If a future edit dropped the `current_epic_source = None` line from the EpicCompleted handler, this test would still pass, silently leaving a stale `SessionDefault` source tag after the epic that field was tracking has completed.",
        "autofix_class": "safe_auto",
        "owner": "review-fixer",
        "confidence": 0.75,
        "evidence": [
          "app.current_epic_source = Some(super::EpicFocusSource::SessionDefault); // set up before firing EpicCompleted",
          "test only asserts `assert_eq!(app.current_epic_id, None);` and the on-disk metadata; no `assert_eq!(app.current_epic_source, None)` follows",
          "epic_workers.rs EpicCompleted arm: `self.current_epic_id = None; self.current_epic_source = None; self.clear_persisted_current_epic_id();` \u2014 the second statement is unverified by the test that appears designed to cover it"
        ],
        "pre_existing": false
      },
      {
        "title": "Three independent foreignness gates collapsed into one weaker checkpoint",
        "severity": "P3",
        "file": "cas-cli/src/ui/factory/director/factory_radar.rs",
        "line": 147,
        "why_it_matters": "factory_radar.rs, mission_epic.rs, and tasks.rs::ScopedTaskView each independently re-checked `epic_has_session_agent_subtask`/`task_assigned_to_session_agent` at render/filter time regardless of how epic_state got set. This diff deletes all three (renderers become 'source-blind' per the stated intent) and relies solely on the focus-resolution layer to have gated correctly beforehand. That consolidation is reasonable in principle, but it removes the safety net that would otherwise have caught exactly the bypass in finding #1 \u2014 previously even a wrongly-set current_epic_id would have been hidden again at render time; now nothing downstream double-checks it. This raises the blast radius of any single bug in the new checkpoint (as findings #1/#2 demonstrate) from 'caught by a second layer' to 'directly visible to the operator'.",
        "autofix_class": "manual",
        "owner": "human",
        "confidence": 0.65,
        "evidence": [
          "factory_radar.rs: removed `if !super::tasks::epic_has_session_agent_subtask(data, focused_epic_id) { render_unfocused_epic_placeholder(...); return; }` and its test now asserts the foreign epic DOES render (`factory_radar_renders_resolved_focused_epic_source_blind`)",
          "mission_epic.rs: removed the `.filter(|epic_id| super::tasks::epic_has_session_agent_subtask(data, epic_id))` before `active_epic` lookup",
          "tasks.rs ScopedTaskView::new: removed `&& group.subtasks.iter().any(|task| task_assigned_to_session_agent(task, data))` from the epic_groups filter, and the renamed test `scoped_task_view_keeps_focused_epic_without_session_agent_subtasks` explicitly asserts the previously-rejected foreign epic group is now kept"
        ],
        "pre_existing": false,
        "requires_verification": true
      }
    ],
    "pre_existing": [],
    "mode": "report-only",
    "intent_summary": "Goal: reconcile factory epic-focus rendering by introducing an explicit EpicFocusSource (Pinned/SessionDefault/Inference) and moving the 'is this focus displayable' gate into resolve_epic_state_for_focus, so pinned/session-default focus always renders while inference-derived focus keeps a foreignness gate \u2014 renderers (factory_radar, mission_epic, ScopedTaskView) become source-blind. Scope: cas-cli/src/ui/factory/{app,daemon,director} \u2014 set_active_epic now returns the previous EpicState for reuse in handle_epic_events, EpicCompleted/reset clears current_epic_id + persisted session-default epic_id without clearing an explicit pin, shared task_matches_agent_filter helper replaces 4 inline closures, plus new read-side metadata / precedence / index-mapping tests. Non-goals: no MCP tool surface, store/schema, or CLI command changes outside the factory TUI epic-focus path.",
    "activation": {
      "activated": [
        "correctness",
        "testing",
        "maintainability",
        "project-standards",
        "adversarial",
        "gpt-5.5:independent"
      ],
      "fallow_skipped": true,
      "fallow_skip_reason": "non-JS/TS repo: Rust codebase (cas-cli), no JS/TS files in the diff",
      "gpt55_independent": true,
      "gpt55_independent_skipped": true,
      "gpt55_independent_skip_reason": "codex exec repeatedly timed out (no output, exit 143, after both 2-minute and 10-minute budgets) despite explicit -s read-only -m gpt-5.5 invocation with codex-cli 0.142.5 present and authenticated-looking on this host \u2014 likely a transient auth/session or environment issue with gpt-5.5 codex in this sandbox, not a prompt problem. Given the instructions, since codex could not run, no independent findings are reported; returning empty findings with this skipped_reason per the persona's step 4/5.",
      "skipped_personas": [
        {
          "reviewer": "gpt-5.5:independent",
          "reason": "codex exec repeatedly timed out (no output, exit 143, after both 2-minute and 10-minute budgets) despite explicit -s read-only -m gpt-5.5 invocation with codex-cli 0.142.5 present and authenticated-looking on this host \u2014 likely a transient auth/session or environment issue with gpt-5.5 codex in this sandbox, not a prompt problem. Given the instructions, since codex could not run, no independent findings are reported; returning empty findings with this skipped_reason per the persona's step 4/5."
        }
      ],
      "personas_run": 5
    },
    "stats": {
      "total_findings": 13,
      "p0": 2,
      "p1": 2,
      "p2": 4,
      "p3": 5,
      "personas_run": 5,
      "task_id": "cas-c9f6"
    }
  }
}
```
