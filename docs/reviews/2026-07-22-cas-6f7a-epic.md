# Integrated review — epic cas-6f7a (comms observability fixes)

**Date:** 2026-07-22
**Diff:** main (73fa659) … epic tip (post fix-round)
**Mode:** interactive, Workflow-backed (7 personas incl. adversarial + gpt-5.5:independent)

## Residual findings and outcomes

1. **P0 — TaskNoteAdded stall fix was dead code** (`crates/cas-factory/src/director.rs:337`, `cas-cli/src/mcp/tools/core/task/notes.rs`). No production code emitted `EventType::TaskNoteAdded`; the whitelist addition was inert and the unit test hand-built the event, bypassing the real handler. Supervisor-verified via repo grep. **Fixed (cas-a851):** `cas_task_notes` now records the event with the caller session, non-fatal on failure; integration tests go through the real handler including a forced-insert-failure case.
2. **P1 — every-init backfill fabricated `delivered` for live legacy rows** (`crates/cas-store/src/prompt_queue_store.rs` init backfill vs `poll_for_target`/`mark_processed` which set `processed_at` without stamping transport delivery). Supervisor-verified. **Fixed (cas-683f7):** backfill gated on `column_exists("prompt_queue","highest_stage")` checked before the `ensure_column` loop (first-migration only); regression test proves post-migration `mark_processed` rows are not promoted.
3. **P2 — release reason stored in `previous_agent_id` lease-history field** (name/doc contradiction, pre-existing column overload). **Deferred:** follow-on task cas-7aef (proper reason column + renderer update).
4. **P2/P3 — no e2e asserting lease-history reason on the normal close path.** **Deferred:** folded into cas-7aef.

## Gates

- Per-branch: full serial workspace gates exit 0 on each worker branch.
- Assembled epic: `cargo test --workspace --no-fail-fast -- --test-threads=1` (ZIG exported, CAS_* env sanitized) — see epic close note for final result. Parallel-run failures in `supervisor_push` are the known pre-existing flake (cas-e838), green in serial/isolation, failure-parity with main.
