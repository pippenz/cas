# cas-code-review: cas-1740 (OSC 8 passthrough)

Report-only review of factory/happy-marten-39 @ a77e8ff vs epic base b4dbb6e.
FIX ROUND (P2 cluster, no P0/P1) — overlay stale-link correctness, per-cell FFI scan per frame, URI sanitization untested, shared-map coupling.

```json
{
  "date": "2026-07-07",
  "task": "cas-1740",
  "branch": "factory/happy-marten-39",
  "commit": "a77e8ff",
  "base_sha": "b4dbb6e",
  "mode": "report-only",
  "verdict": "FIX ROUND (P2 cluster, no P0/P1) \u2014 overlay stale-link correctness, per-cell FFI scan per frame, URI sanitization untested, shared-map coupling.",
  "envelope": {
    "residual": [
      {
        "title": "OSC 8 URI escape-sequence sanitization has zero test coverage",
        "severity": "P2",
        "file": "cas-cli/src/ui/factory/buffer_backend.rs",
        "line": 84,
        "why_it_matters": "write_osc8_open() strips b'\\x1b' and b'\\x07' from the URI before embedding it between OSC 8 escape delimiters \u2014 this is the only thing preventing a corrupted or adversarial hyperlink URI (sourced from pane/terminal content via ghostty_vt) from injecting raw ESC/BEL bytes into the host terminal's escape-sequence stream. None of the four buffer_backend tests (linked_cells_emit_osc8_around_contiguous_runs, different_links_close_and_reopen_at_run_boundaries, unlinked_cells_emit_no_osc8, cursor_jumps_close_active_link) use a URI containing ESC or BEL bytes, so if this .filter(...) guard were removed, weakened, or the byte set typo'd (e.g. b'\\x1a' instead of b'\\x1b'), every existing test would still pass while raw escape/BEL bytes leaked straight into the rendered frame.",
        "autofix_class": "manual",
        "owner": "review-fixer",
        "confidence": 0.9,
        "evidence": [
          "fn write_osc8_open(&mut self, uri: &str) { ... self.buffer.extend(uri.bytes().filter(|b| !matches!(b, b'\\x1b' | b'\\x07'))); ... }",
          "all four #[cfg(test)] cases in buffer_backend.rs insert plain 'https://example.com'-style strings with no control bytes"
        ],
        "pre_existing": false
      },
      {
        "title": "Per-cell FFI hyperlink scan runs unconditionally on every render frame",
        "severity": "P2",
        "file": "crates/cas-mux/src/pane/mod.rs",
        "line": 810,
        "why_it_matters": "row_hyperlinks() issues one unsafe FFI call (ghostty_vt_terminal_hyperlink_at) per column for every row requested, with no fast path to skip panes/rows that contain zero hyperlinks. record_pane_hyperlinks (core.rs) invokes this for every visible row of every visible pane (supervisor, worker tab, and each mission-control pane) on every single render() call, and render() runs on every UI tick (keystrokes, streamed worker output, periodic refresh). For a multi-worker factory session at ~100x40 visible cells per pane, that is thousands of unsafe cross-FFI calls per frame purely to discover 'no links here' in the overwhelmingly common case where no hyperlink is present anywhere on screen. The codebase already has a bulk-per-row pattern for exactly this kind of query (Terminal::screen_row_style_runs, one FFI call returning a Vec for the whole row) that this new API does not reuse, so the N+1-per-cell shape was avoidable.",
        "autofix_class": "manual",
        "owner": "downstream-resolver",
        "confidence": 0.85,
        "evidence": [
          "pub fn row_hyperlinks(&self, row: u16) -> Vec<Option<String>> { (0..self.cols).map(|col| self.terminal.hyperlink_at(col + 1, row + 1)).collect() } (crates/cas-mux/src/pane/mod.rs:808-812)",
          "for row in 0..area.height { let row_links = pane.row_hyperlinks(row); ... } called from render_supervisor_view, the single worker-tab view, and the mission-control panes loop, i.e. once per visible pane per render() (cas-cli/src/ui/factory/app/render_and_ops/rendering/core.rs:24-31)",
          "pub fn hyperlink_at(&self, col: u16, row: u16) -> Option<String> { let bytes = unsafe { sys::ghostty_vt_terminal_hyperlink_at(...) }; ... String::from_utf8_lossy(slice).into_owned() } \u2014 crosses into C and heap-allocates a new String whenever a link is present, still called even when the answer is None (crates/ghostty_vt/src/lib.rs:501-516)",
          "existing bulk-fetch precedent in the same struct: screen_row_style_runs() makes a single FFI call per row and returns a Vec<StyleRun>, contrasting with the new per-cell hyperlink_at loop (crates/ghostty_vt/src/lib.rs:478-498)"
        ],
        "pre_existing": false
      },
      {
        "title": "Overlay dialogs inherit stale pane hyperlink coordinates from the underlying frame",
        "severity": "P2",
        "file": "cas-cli/src/ui/factory/app/render_and_ops/rendering/core.rs",
        "line": 102,
        "why_it_matters": "render() calls self.clear_pane_hyperlinks() exactly once, before dispatching to render_panes_view(). render_panes_view() then records hyperlink coordinates while drawing worker/supervisor panes (record_pane_hyperlinks), and only afterward calls self.render_overlays(frame) to paint dialogs (help/inject/task-detail/changes/terminal/feedback/reminder) on top of the same Buffer. Ratatui assembles all render_widget calls for the frame into one Buffer before Terminal::draw() diffs it and invokes BufferBackend::draw() once with the final content. Because pane_hyperlinks is never re-cleared or filtered after render_overlays runs, any dialog cell that happens to land on a screen coordinate previously recorded as a pane hyperlink will be looked up in BufferBackend::draw() against the STALE (x,y) entry from the covered pane, wrapping unrelated dialog text in an OSC 8 escape pointing at another worker's/old URL. This directly undermines the feature's own correctness: showing any overlay over a pane whose visible cells contained an OSC8 link (e.g. a worker running `git`/`rg --hyperlink`/similar tools) makes dialog text incorrectly ctrl-clickable to a stale URL until the dialog is dismissed and the pane repaints that region.",
        "autofix_class": "manual",
        "owner": "human",
        "confidence": 0.65,
        "evidence": [
          "core.rs: `pub fn render(&mut self, frame: &mut Frame) { self.clear_pane_hyperlinks(); ... match self.factory_view_mode { FactoryViewMode::Panes => self.render_panes_view(frame), ... } }` \u2014 clear happens exactly once, before any pane content (and before overlays) are drawn.",
          "core.rs render_panes_view(): calls render_workers/render_supervisor (which call record_pane_hyperlinks) and THEN `self.render_overlays(frame);` as the final step, with no intervening clear or hyperlink-map update for the overlay-covered region.",
          "render_overlays() (unchanged by this diff) renders help/inject/task/changes/terminal/feedback dialogs via frame.render_widget on areas that can overlap previously-rendered pane regions; none of those dialog render paths call record_pane_hyperlinks or clear_pane_hyperlinks.",
          "BufferBackend::draw() (buffer_backend.rs) looks up `hyperlinks.get(&(x,y))` purely by absolute coordinate for whatever cell content the final assembled Buffer produced at that position, with no knowledge of which widget last owned that cell."
        ],
        "pre_existing": false
      },
      {
        "title": "Shared HyperlinkMap couples full and compact render pipelines via implicit ordering",
        "severity": "P2",
        "file": "cas-cli/src/ui/factory/app/mod.rs",
        "line": 476,
        "why_it_matters": "pane_hyperlinks is a single Arc<Mutex<BTreeMap<(u16,u16),String>>> cloned into both the main-terminal BufferBackend and the compact-terminal BufferBackend. render() and render_compact() each independently clear_pane_hyperlinks() then repopulate coordinates in their own (differently-sized) grid before their own backend drains the map via draw(). Correctness today depends entirely on an unenforced, undocumented invariant: the event loop must always finish one full clear+populate+drain cycle before the other starts (currently guaranteed only because terminal.draw(render) runs to completion before ct.draw(render_compact) in the same synchronous tick in lifecycle.rs). Nothing in the type system, the field's doc-comment, or a test protects that ordering. A future change - parallelizing the two draws, adding a third consumer, or reordering the two draw calls - would silently cross-contaminate hyperlink coordinates between the full and compact layouts with no compiler or test signal.",
        "autofix_class": "manual",
        "owner": "human",
        "confidence": 0.6,
        "evidence": [
          "cas-cli/src/ui/factory/app/mod.rs: `pane_hyperlinks: HyperlinkMap` field doc says \"Per-frame OSC 8 hyperlink metadata keyed by final host terminal cell\" with no mention it backs two independent terminals",
          "cas-cli/src/ui/factory/daemon/runtime/lifecycle.rs: both `BufferBackend::with_hyperlinks(self.cols, self.rows, self.app.pane_hyperlink_map())` (main) and `BufferBackend::with_hyperlinks(cc, cr, self.app.pane_hyperlink_map())` (compact) clone the same Arc from self.app.pane_hyperlinks",
          "cas-cli/src/ui/factory/daemon/runtime/lifecycle.rs: `terminal.draw(|f| self.app.render(f))` runs, then later in the same `if dirty` block `ct.draw(|f| self.app.render_compact(f))` runs - correctness relies on this fixed sequential order, not on any owned/scoped state",
          "cas-cli/src/ui/factory/app/render_and_ops/rendering/core.rs: render() calls self.clear_pane_hyperlinks() at its own top, and render_compact() independently calls self.clear_pane_hyperlinks() at its own top, each assuming exclusive access to the shared map for its duration"
        ],
        "pre_existing": false
      },
      {
        "title": "record_pane_hyperlinks column-count mismatch (area.width vs pane cols) untested",
        "severity": "P3",
        "file": "cas-cli/src/ui/factory/app/render_and_ops/rendering/core.rs",
        "line": 26,
        "why_it_matters": "The loop bounds via `area.width.min(row_links.len() as u16)` before indexing `row_links[col as usize]`, which is the only guard against an out-of-bounds panic when a pane's rendered Rect.width differs from the pane's actual column count (row_hyperlinks() returns a Vec sized to self.cols). The only existing test (pane_hyperlinks_are_recorded_at_final_screen_offsets_for_two_panes) uses Rect.width == 20 for panes created with exactly 20 cols, so the mismatch branch is never exercised \u2014 a regression that dropped the `.min(...)` or swapped its operands would not be caught by any test and could panic at runtime on any resize race between layout area and pane dimensions.",
        "autofix_class": "manual",
        "owner": "review-fixer",
        "confidence": 0.8,
        "evidence": [
          "for col in 0..area.width.min(row_links.len() as u16) { if let Some(uri) = row_links[col as usize].as_ref() { ... } }",
          "test only constructs Pane::director(\"left\", 1, 20) / (\"right\", 1, 20) and passes Rect { width: 20, ... } for both \u2014 cols always equals area.width"
        ],
        "pre_existing": false
      },
      {
        "title": "record_pane_hyperlinks row/y-offset math only exercised at row=0",
        "severity": "P3",
        "file": "cas-cli/src/ui/factory/app/render_and_ops/rendering/core.rs",
        "line": 24,
        "why_it_matters": "The loop computes `(area.x + col, area.y + row)` for row in 0..area.height, but the sole test uses Rect { height: 1, ... } for every pane, so `row` is always 0 and the y-offset addition is never verified across multiple rows. A regression that hard-codes row 0, transposes row/col, or breaks multi-row accumulation (e.g. only the last row's links survive) would still pass the current test suite.",
        "autofix_class": "manual",
        "owner": "review-fixer",
        "confidence": 0.75,
        "evidence": [
          "for row in 0..area.height { let row_links = pane.row_hyperlinks(row); ... hyperlinks.insert((area.x + col, area.y + row), uri.clone()); }",
          "test Rect literals for both 'left' and 'right' panes use height: 1, so the loop body executes exactly once per pane at row == 0"
        ],
        "pre_existing": false
      },
      {
        "title": "Identical 3-line 'close active link' block duplicated 3x in BufferBackend::draw",
        "severity": "P3",
        "file": "cas-cli/src/ui/factory/buffer_backend.rs",
        "line": 143,
        "why_it_matters": "The exact same guard `if active_link.take().is_some() { self.write_osc8_close(); }` appears verbatim at the cursor-jump check, inside the per-cell link-change check, and once more after the draw loop to flush a trailing link. Any future change to how a link close is emitted requires remembering to touch three separate call sites instead of one, which is the classic setup for a forgotten-update bug later.",
        "autofix_class": "safe_auto",
        "owner": "review-fixer",
        "confidence": 0.75,
        "evidence": [
          "cas-cli/src/ui/factory/buffer_backend.rs:143 `if active_link.take().is_some() { self.write_osc8_close(); }` (cursor jump)",
          "cas-cli/src/ui/factory/buffer_backend.rs:199 same block (per-cell link change)",
          "cas-cli/src/ui/factory/buffer_backend.rs:212 same block (post-loop trailing flush)"
        ],
        "pre_existing": false
      },
      {
        "title": "Hyperlink URIs are duplicated per-cell then re-cloned wholesale every draw() call",
        "severity": "P3",
        "file": "cas-cli/src/ui/factory/buffer_backend.rs",
        "line": 136,
        "why_it_matters": "HyperlinkMap stores an owned String per (x,y) cell rather than a shared/interned handle, so record_pane_hyperlinks already clones the same URI once for every cell a multi-character link covers (core.rs:28), and then BufferBackend::draw clones the *entire* map \u2014 including every one of those per-cell Strings \u2014 again on every single draw() call (i.e. every rendered frame), even when the link set is unchanged from the previous frame. Using Arc<str>/Rc<str> as the map value would make both the per-cell insert and the per-frame snapshot O(1) refcount bumps instead of repeated heap allocations, avoiding wasted allocator churn that scales with total hyperlinked-cell count and frame rate.",
        "autofix_class": "manual",
        "owner": "downstream-resolver",
        "confidence": 0.7,
        "evidence": [
          "pub type HyperlinkMap = Arc<Mutex<BTreeMap<(u16, u16), String>>>; (cas-cli/src/ui/factory/buffer_backend.rs:25)",
          "let hyperlinks = self.hyperlinks.lock().map(|map| map.clone()).unwrap_or_default(); \u2014 clones the whole map (and every String value) once per draw() call (cas-cli/src/ui/factory/buffer_backend.rs:133-137)",
          "hyperlinks.insert((area.x + col, area.y + row), uri.clone()); \u2014 clones the same URI once per covered cell every frame (cas-cli/src/ui/factory/app/render_and_ops/rendering/core.rs:28)"
        ],
        "pre_existing": false
      }
    ],
    "pre_existing": [],
    "mode": "report-only",
    "intent_summary": "Goal: re-emit OSC 8 hyperlink escape sequences at the factory TUI frame writer so terminal-rendered pane links become ctrl+clickable in the host terminal, sourced from ghostty_vt's per-cell hyperlink data with pane-offset correction into a shared per-frame HyperlinkMap. Scope: cas-cli factory app/init/rendering, BufferBackend::draw (open/close bracketing + URI sanitization), daemon lifecycle wiring, and a new Pane::row_hyperlinks accessor in cas-mux. Non-goals: no mouse capture is introduced, no changes to task/coordination/store/MCP-dispatch logic \u2014 this is purely a rendering/escape-sequence passthrough feature with matching byte-stream unit tests.",
    "activation": {
      "activated": [
        "correctness",
        "testing",
        "maintainability",
        "project-standards",
        "performance",
        "gpt-5.5:independent"
      ],
      "fallow_skipped": true,
      "fallow_skip_reason": "non-JS/TS repo: no package.json and no JS/TS files in the diff \u2014 this is a Rust codebase (cas-cli/crates/cas-mux)",
      "gpt55_independent": true,
      "gpt55_independent_skipped": false,
      "gpt55_independent_skip_reason": null,
      "skipped_personas": [],
      "personas_run": 6
    },
    "stats": {
      "total_findings": 8,
      "p0": 0,
      "p1": 0,
      "p2": 4,
      "p3": 4,
      "personas_run": 6,
      "task_id": "cas-1740"
    }
  }
}
```
