---
id: rule-044
---

Choose subagent vs skill based on context needs: Use SUBAGENT when all needed context can be passed in the prompt or the agent fetches its own data by ID. Use SKILL when the parent has conversation context the skill needs or session history is important.