---
id: rule-036
paths: "src/mcp/tools/types.rs"
---

Document fields and functions in MCP tool request types with #[schemars(description = "...")] annotations - these generate JSON schemas for parameters and some fields are defined for schema completeness but may not be read directly